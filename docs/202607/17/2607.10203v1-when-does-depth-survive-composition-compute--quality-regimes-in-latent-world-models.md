---
title: When Does Depth Survive Composition? Compute--Quality Regimes in Latent World Models
title_zh: 深度何时能在组合中生存？潜在世界模型中的计算-质量机制
authors: Achyuthan Sivasankar
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10203v1"
tags: ["query:wmi"]
score: 8.0
evidence: 分析潜在世界模型中深度组合对预测轨迹的影响
tldr: 自适应计算世界模型假设深度能改善预测且可路由，但在自回归组合中可能失效。提出浅层惩罚ρ=浅层退出展开误差/全深度展开误差，在9个DeepMind Control任务上发现三种机制：内在型（深度有助，ρ高达4.7倍）、反转型（浅层胜出，ρ低至0.85倍）和平坦型。反转由训练引起，且存在路由性catch-22：使出口可路由的监督同时训练它们超越全深度。ρ与观测/动作维度和一步模型误差高度相关，预测规划中深度的效用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10203v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1220, \"height\": 683, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1397, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 634, \"height\": 465, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 657, \"height\": 455, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 717, \"height\": 557, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 628, \"height\": 452, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1228, \"height\": 431, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1315, \"height\": 514, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1413, \"height\": 1162, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10203v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1425, \"height\": 559, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10203v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 965, \"height\": 426, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10203v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1453, \"height\": 186, \"label\": \"Table\"}]"
motivation: 探究深度预测器在自回归组合中保持精确性的条件，以及何时路由深度能提升世界模型性能。
method: 设计浅层惩罚ρ，在九个DeepMind Control任务上对比单步和多步训练下的浅层退出与全深度展开误差。
result: 发现三种机制：6/9任务深度有益（内在），2/9浅层反超（反转，如cheetah），1/9平坦；反转由训练引起且与维度、误差强相关。
conclusion: 深度有效性依赖度量空间、展开步长和编码器；反转任务中浅层规划更优，警示自适应路由需谨慎评估组合效应。
---

## 摘要
自适应计算的世界模型——每步使用可变深度的早期退出或混合深度预测器——假设深度能带来更好的预测，并且可以自适应地路由。在自回归滚动中，第一个假设要求深度的每步精度能够在组合中生存。我们通过预先注册的工具——浅层惩罚ρ=err(最浅退出滚动)/err(全深度滚动)——在九个DeepMind Control任务上，在匹配的单步（K=1）和多步（K=4）训练下（各三个种子）进行测试。我们发现三种机制：在6/9的任务中深度有助于滚动（内在，ρ高达4.7倍），在2/9的任务中浅层退出优于全栈（反转，ρ低至0.85倍），还有一个是平坦的。鲁棒的反转（cheetah）并非动力学的属性，而是由训练产生的：一个仅在第一个滚动步骤监督早期退出的消融实验消除了它（ρ：0.87→1.18，n=8，Δ=+0.31），而一个内在权衡任务则不受影响——我们称之为可路由性第22条军规的双重分离，因为使退出可路由的监督正是训练它们胜过全栈滚动的监督。该机制在一定程度上可先验预测：观测/动作维度和单步模型误差与ρ的相关性为|斯皮尔曼|≈0.75（n=9）。在CEM规划器内部，ρ的符号预测规划是否受益于深度，在最尖锐的反转任务上，浅层规划优于深层规划。最后，三个注意事项：任务的机制取决于度量空间、滚动范围和编码器。所有阈值和门控在计算活动之前已固定，包括为激发本研究假设的预先注册的反面结果。

## Abstract
Adaptive-compute world models -- early-exit or mixture-of-depths predictors that spend variable depth per step -- assume depth buys better predictions and can be routed adaptively. In autoregressive rollouts, the first assumption requires depth's per-step precision to survive composition. We test this with a pre-registered instrument, the shallow penalty $ρ=\mathrm{err}(\text{shallowest-exit rollout})/\mathrm{err}(\text{full-depth rollout})$, across nine DeepMind Control tasks under matched single-step ($K=1$) and multi-step ($K=4$) training, three seeds each. We find three regimes: on 6/9 tasks depth helps rollouts (intrinsic, $ρ$ up to $4.7\times$), on 2/9 the shallow exits beat the full stack (inversion, $ρ$ down to $0.85\times$), and one is flat. The robust inversion (cheetah) is not a property of the dynamics but is created by training: an ablation supervising early exits only at the first rollout step erases it ($ρ: 0.87\to1.18$, $n=8$, $Δ=+0.31$), while an intrinsic-tradeoff task is unaffected -- a double dissociation we call the routability catch-22, since the supervision that makes exits routable is what trains them to out-roll the full stack. The regime is partly predictable a priori: observation/action dimensionality and one-step model error correlate with $ρ$ at $|\text{Spearman}|\approx0.75$ ($n=9$). Inside a CEM planner, $ρ$'s sign predicts whether planning benefits from depth, most sharply on the inversion task, where shallow planning beats deep. Finally, three cautions: a task's regime depends on the metric space, the rollout horizon, and the encoder. All thresholds and gates were fixed before the compute campaign, including a pre-registered negative for the hypothesis that motivated the study.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：自适应计算世界模型（如早期退出、混合深度）依赖两个前提：①深度增加能带来更好的单步预测；②深度可以在每个步自适应路由。在自回归滚动（planning模式下对同一编码观测多次调用预测器）中，深度带来的逐步精度能否在组合后仍然保持？该研究直接检验了这一假设。
- **背景**：潜在世界模型（如JEPA）预测潜在动态，规划时需多次滚动；自适应计算可降低计算成本。但现有工作默认深层预测更优，忽略了组合效应。
- **核心问题**：深度在自回归组合中是否仍然有效？什么条件下深度有益、有害或无关？

## 2. 论文提出的方法论

- **核心思想**：设计一个简单度量——**浅层惩罚ρ** = err(最浅退出滚动) / err(全深度滚动)。ρ > 1 表示深度有益（内在机制）；ρ ≈ 1 表示平坦（无差异）；ρ < 1 表示浅层反超（反转机制）。
- **技术细节**：
  - 使用12层残差块预测器，每2层设置早期退出头（深度2,4,6,8,10,12）。
  - 编码器为MLP（2层），潜在维度64，使用EMA目标。
  - 损失：潜在MSE + 深度监督 + 蒸馏 + VICReg正则化。
  - 训练模式：单步（K=1） vs 多步（K=4，潜在超调，通过4步全深度滚动回传梯度）。
  - 预先注册分类阈值：内在 ρ ≥ 1.25（K=1）；反转 ρ < 1.15（K=1）且 ρ < 0.90（K=4）；平坦 ρ < 1.15 且 0.90 ≤ ρ < 1.25；制造（多步创造权衡）等。
- **计算量**：浅层（2层）0.57 MMac，全深度（12层）3.19 MMac，快5.6倍。

## 3. 实验设计

- **数据集/场景**：9个DeepMind Control Suite任务（DMC）：acrobot, cartpole, cheetah, finger, hopper, humanoid, quadruped, reacher, walker。每个任务使用随机策略收集80条轨迹（长度1000）作为训练数据，模型无关。
- **Benchmark**：滚动误差对比固定深度（2,4,...,12）在H=10步的潜在误差（相对全深度），并额外在观测空间（线性探针解码）验证。
- **对比方法**：
  - 不同深度固定滚动对比（自身全深度为分母）。
  - 训练模式对比：K=1 vs K=4。
  - 消融实验：第一步骤仅监督（first-step-only） vs 标准全步骤监督。
  - 编码器对比：MLP（状态） vs CNN（像素64×64）。
  - 规划验证：在CEM规划器中比较深度2、深度12和路由规划。
  - 无规划奖励模型不可用的任务被过滤（hopper, reacher）。

## 4. 资源与算力

- **训练算力**：
  - 主要训练：78个训练运行在H100/H200 GPU上（原始3任务+扩展6任务+消融+像素实验）。
  - 额外13个训练运行（四足消融和cheetah种子扩展）在Apple Silicon MPS设备上。
  - 总计91次训练运行。
  - 每个任务训练步数：60k步（humanoid为80k），batch size 256。
- **未详细说明**：具体GPU数量、单卡型号、总耗时未给出。

## 5. 实验数量与充分性

- **实验组数**：
  - 原始3任务（cheetah, humanoid, walker）× K∈{1,4} × 3种子 = 18。
  - 扩展6任务 × K∈{1,4} × 3种子 = 36。
  - 消融实验：cheetah+walker（各3种子）+ quadruped（3种子）= 6+3=9。
  - cheetah种子扩展：标准+消融各加5种子，共10。
  - 像素实验：3任务 × K∈{1,4} × 3种子 = 18。
  - 规划验证：针对所有可运行任务，共6个任务有奖励模型（其中2个不可用，4个实际运行）。
- **充分性与公平性**：
  - 预先注册所有阈值和门控，机械执行，避免事后选择。
  - 每个任务3种子，种间分布显示标签稳定（除四足外均一致）。
  - 使用匹配的单步/多步训练（相同数据、相同顺序，仅损失不同），确保因果可比。
  - 消融实验设计严谨：第一步骤监督消融 cheetah 显著逆转（Δ=+0.31，分布不重叠），而内在任务不受影响，双重分离。
  - 像素实验控制模型质量（walker一步误差一致），表明机制非编码器伪影。
- **局限性**：n=9相关性分析样本小；部分规划验证边缘（四足未通过，walker奖励弱）；平坦机制仅humanoid一个成员。

## 6. 论文的主要结论与发现

- **三种机制**：
  - **内在机制**（6/9任务）：深度有益，ρK1 ≥ 1.25，如 acrobot (3.34)、finger (4.74)、walker (1.67)。
  - **反转机制**（2/9任务）：浅层反超，如 cheetah (ρK4=0.85)、quadruped (0.89)。
  - **平坦机制**（1/9任务）：humanoid (ρ≈0.96)。
- **反转因果机制**：多步训练中的深度监督导致反转。**可路由性第22条军规**：使退出可路由的逐步监督同时训练它们胜过全深度滚动。消融实验消除 cheetah 反转（ρ从0.87升至1.18），但四足不受影响（机制任务特异性）。
- **三个评估注意事项**：
  - 度量空间依赖：humanoid在潜在空间平坦，在观测空间反转（0.78）。
  - 滚动范围依赖：cheetah反转短期存在，H=50时恢复（ρ升至1.07）。
  - 编码器依赖：状态MLP下的内在任务在像素CNN下变平坦（cartpole ρ从2.15降为0.85，walker从1.67降为0.87）。
- **可预测性**：ρ与观测维度（斯皮尔曼-0.77）、动作维度（-0.73）、全深度一步误差（-0.77）相关；高维、难建模任务倾向于反转/平坦。
- **规划闭环验证**：ρ的符号预测规划收益。cheetah（反转）浅层规划优于深层（7.24 vs 6.58）；cartpole（内在）深层优于浅层（+32回报）；acrobot（内在）类似。P1在4/6任务成立，P2通过校准路由部分实现（acrobot全部种子通过，cartpole边界）。
- **最初假设失败**：多步训练并未制造深度权衡（0/3任务为“制造”状态），该研究因此转为描述性分类。

## 7. 优点

- **方法简洁直接**：单一度量ρ有效量化深度组合效应，且计算成本低（单次审计）。
- **预先注册与机械执行**：提前固定阈值、门控和决策规则，避免后见偏差，包括原假设的明确空结果。
- **因果消融设计**：通过第一步骤监督消融隔离反转机制，双重分离设计（cheetah vs walker）证明训练特异性。
- **多维度验证**：从潜在误差到观测误差、不同滚动范围、编码器、再到闭环规划，形成完整证据链。
- **发现实用警示**：三个评估注意事项（度量空间、滚动范围、编码器）对世界模型社区具有直接方法论价值。

## 8. 不足与局限

- **任务与表示局限**：仅在状态空间DMC任务（MLP编码器）上系统研究；像素实验仅对比三个任务，且部分像素模型未充分训练（如cartpole K=4）。
- **平坦成员单一**：仅humanoid一个平坦任务，且其标签在潜在与观测空间不一致，导致分类脆弱。
- **阈值人为预设**：ρ阈值（1.25, 1.15, 0.90）缺乏理论基础，虽报告原始值以便他人调整。
- **反转机制非通用**：可路由性第22条军规仅解释cheetah的鲁棒反转，quadruped的边际反转未被消融影响，说明存在其他原因。
- **可预测性样本小**：n=9的相关性伴随共线性预测因子，仅为描述性关联，需外推测试（已预注册6个任务外推实验但未完成）。
- **规划验证部分失败**：P1在四足和walker上偏离（前者边际反转、后者奖励弱）；P2固定阈值失败，校准后部分通过；两个任务因奖励稀疏不可用。
- **计算资源报告不完整**：GPU型号数量、总训练时长未详细列出。

（完）
