---
title: "Anticipate Before Acting: Future-State-Conditioned Vision-Language Navigation"
title_zh: 行动前先预测：未来状态条件化的视觉语言导航
authors: "Lingfeng Zhang, Zhanguang Zhang, Liheng Ma, Tongtong Cao, Yingxue Zhang"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18042v2"
tags: ["query:wmi"]
score: 8.0
evidence: 用于行为克隆的未来状态条件策略，包含预测性内部模型
tldr: 标准行为克隆仅监督下一步动作，不鼓励策略预测未来视觉结果，限制了长程导航决策。为此提出FSC-VLN，在因果策略中引入未来查询token，训练时与未来Δ步的冻结视觉嵌入对齐，将未来信息蒸馏到策略状态中，推理仅需历史观测。在R2R val-unseen上，该方法显著提升SR/OSR/SPL，尤其在长程片段上增益更大。该设计仅增加两个前缀token，轻量有效，实现了可部署的未来状态条件视觉语言导航。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18042v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1429, \"height\": 685, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18042v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1361, \"height\": 410, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1443, \"height\": 223, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1261, \"height\": 361, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1291, \"height\": 248, \"label\": \"Table\"}]"
motivation: 标准行为克隆不鼓励策略预测未来视觉结果，限制了长程决策；而访问未来图像能大幅提升导航，启发蒸馏未来信息到策略状态。
method: 在因果VLN策略中加入未来查询token，训练时将其表示与Δ步后的冻结视觉嵌入对齐，推理仅用过去和当前观测，仅增加两个学习前缀token。
result: 在R2R val-unseen上，FSC-VLN在两种训练数据设置下均提升SR/OSR/SPL，长程片段增益更显著，消融验证了双查询设计的必要性。
conclusion: 提出轻量未来状态条件VLN方法，通过训练时未来监督蒸馏信息，显著改善长程导航决策，保持基线推理效率。
---

## 摘要
端到端视觉语言导航（VLN）结合因果视觉语言模型，直接将指令和以自我为中心的观察映射到行动，但标准的行为克隆仅监督下一个行动，并未明确鼓励策略状态预测未来的视觉结果，从而限制了长期决策。一项特权输入诊断表明，访问专家轨迹的未来图像可以显著改善导航，这表明未来观察包含丰富、可操作的线索，尽管在部署时无法获得此类输入。受此信号启发，我们提出了未来状态条件化VLN（FSC-VLN），这是一种可部署的模型，它通过一个未来查询令牌增强因果策略，并使用仅训练时的未来状态监督，将来自未来观察的信息提炼到策略状态中。具体而言，在训练期间，我们将未来查询表示与$Δ$步后的冻结视觉嵌入对齐，而推理仅需过去和当前的观察。这种设计保留了基准推理模式，仅添加了两个学习的前缀令牌，意味着最小的开销。在R2R val-unseen上，FSC-VLN在两种训练数据机制下，相比于StreamVLN风格的基准，在SR/OSR/SPL上均有提升，尤其在长视野任务中增益更大；消融实验进一步支持了分离未来和行动查询的双查询设计。

## Abstract
End-to-end vision-language navigation (VLN) with causal vision-language models maps instructions and egocentric observations directly to actions, but standard behavior cloning supervises only the next action and does not explicitly encourage the policy state to be predictive of future visual outcomes, limiting long-horizon decision making. A privileged-input diagnostic shows that access to an expert-trajectory future image can substantially improve navigation, indicating that future observations contain rich, actionable cues, though such inputs are unavailable at deployment. Motivated by this signal, we propose Future-State-Conditioned VLN (FSC-VLN), a deployable model that augments a causal policy with a future-query token and uses training-only future-state supervision to distill information from future observations into the policy state. Concretely, during training we align the future-query representation to a frozen visual embedding $Δ$ steps ahead, while inference requires only past and current observations. This design preserves the baseline inference pattern and adds only two learned prefix tokens, implying minimal overhead. On R2R val-unseen, FSC-VLN improves SR/OSR/SPL over a StreamVLN-style baseline under two training-data regimes, with larger gains on long-horizon episodes; ablations further support the dual-query design that separates future and action queries.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在端到端视觉语言导航（VLN）中，现有基于因果视觉语言模型的方法（如StreamVLN）采用行为克隆训练，仅监督当前动作，不明确鼓励策略内部状态预测未来视觉观察。这导致长程决策中微小误差累积，尤其在连续环境（VLN-CE）中，策略需要维持指令进展、理解动态场景并从错误中恢复。
- **核心问题**：能否在不使用未来观测（部署时不可得）的情况下，通过训练时引入未来状态监督来提升可部署的在线策略？作者首先通过特权输入诊断确认：在训练和测试时都提供专家轨迹的未来图像（Δ=32步）可大幅提升导航成功率（SR从29.96%→71.77%），验证了未来观测包含可操作的线索，但无法直接用于部署。由此启发将未来信息蒸馏到策略状态中。
- **整体含义**：提出一种轻量级方法，在因果VLN策略中加入未来查询token，训练时将其表示与冻结的视觉编码器提取的未来Δ步观察对齐，推理时仅需过去和当前观测，仅增加两个前缀token，实现可部署的未来状态条件化导航。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：通过训练时引入未来状态预测作为辅助目标，迫使策略内部表示对未来视觉结果具有预测性，从而在不改变推理模式的情况下提升长程导航决策质量。
- **关键技术细节**：
  - **模型架构**：基于Qwen2-7B因果Transformer，使用冻结的SigLIP视觉编码器（含MLP投影器）。在每个观测步骤，将指令、历史观测和当前观测编码为多模态上下文。
  - **双查询设计**：在上下文后插入两个可学习前缀token：**未来查询qF**和**动作查询qA**。因果序列为：`Ct → qF → qA → at`。qF负责预测未来状态，qA可注意力到qF，用于生成动作。
  - **未来状态目标构建**：训练时，通过共享的冻结视觉编码器将未来观测ot+Δ（Δ=32步）编码为视觉特征，进行mean pooling得到目标嵌入z*_t+Δ，并施加stop-gradient。对未来查询hFt使用投影后的L1对齐损失：L_future = ||g(hFt) - g(z*_t+Δ)||_1，其中g为小型投影网络。
  - **训练目标**：总损失 = LLM（因果语言建模交叉熵）+ λF * L_future，λF=0.1。仅优化因果LM、LM head和查询嵌入；冻结视觉编码器和投影器。
  - **推理**：丢弃未来目标分支，多模态上下文与两个查询一起预处理，未来查询无需自回归解码，动作查询注意力到未来查询前缀，LM head自回归解码动作token。无额外未来观测或辅助编码器。
  - **复杂度**：仅增加两个前缀token，推理成本接近基线。

### 3. 实验设计：数据集、benchmark与对比方法

- **数据集与场景**：
  - 主要使用R2R（Room-to-Room）和RxR（Room-across-Room）室内导航数据集，评估于R2R val-unseen（1839个episode）。
  - 训练数据两种设置：① R2R+RxR；② R2R+RxR+EnvDrop+ScaleVLN（数据增强和额外数据）。
  - 按轨迹长度分层：短程（geodesic≤10m，1293个episode）和长程（>10m，546个episode）。
- **benchmark与方法对比**：
  - **基线**：StreamVLN风格端到端VLM策略，其配置（骨干网络、优化器、学习率调度、批大小、数据预处理、解码设置）与FSC-VLN保持一致以确保公平。
  - **特权诊断实验**：将专家轨迹未来图像作为额外输入（测试时也提供），纯诊断用途。
  - **主实验**：对比FSC-VLN与基线在SR、OSR、SPL上的表现。
- **对比方法**：文中未与其他SOTA方法（如NaVid、Uni-NaVid等）对比整体性能，主要聚焦于与自身基线的改善及消融。

### 4. 资源与算力

- 文中明确提到训练使用**4块GPU**，每个设备批大小为1，梯度累积16步（全局批大小为64），使用bfloat16精度、梯度检查点、DeepSpeed ZeRO-2。
- 训练1个epoch（轨迹片段长度不超过32步），优化器为AdamW（学习率2×10⁻⁵，权重衰减0，预热比例0.075，余弦调度，梯度裁剪1.0）。
- **但未明确给出训练时长（如小时数）**，也未说明GPU具体型号（推测为常见高性能GPU如A100或V100）。

### 5. 实验数量与充分性

- **实验组数**：主要包含5组实验：
  1. 特权输入诊断实验（表1）：单组对比。
  2. 主实验（表2）：两种数据设置下对比基线与FSC-VLN。
  3. 长/短程分层分析（表3）：两种数据设置下的分层结果。
  4. 消融实验（表4）：移除动作查询（仅保留未来查询）对比完整双查询设计。
  5. 此外本文仅含上述实验，未见更多消融（如不同Δ值、不同λF、不同损失函数等）。
- **充分性分析**：
  - **优点**：实验设计合理，分层分析揭示了方法对长程轨迹的特有增益，消融验证了双查询设计的必要性。结果统计全面（SR/OSR/SPL）。
  - **不足**：仅在单一骨干（Qwen2-7B）上验证，未在不同VLM模型、不同环境（如VLN-CE其他分割、REVERIE、CVDN等）中比较；未对比最近的其他未来预测方法（如VLA-JEPA、DreamZero等）；未进行超参数敏感性分析（Δ值仅固定为32）；未报告推理速度或计算成本具体数值，仅定性说明。

### 6. 论文的主要结论与发现

- 未来观测包含可操作的视觉证据，通过特权输入诊断确认（SR提升41.81个百分点）。
- FSC-VLN在两种数据设置下均一致提升基线：R2R+RxR下SR+1.47、OSR+3.27、SPL+1.11；增加扩大数据后提升较小（SR+0.98等）。
- 收益主要集中于长程轨迹：长程SR提升2.93–3.85个百分点，SPL提升2.52–3.70；短程几乎不变。
- 双查询设计优于单查询：单独未来查询（无动作查询）导致SR和SPL下降，尤其在长程任务中（SR下降1.28个百分点），说明分离未来状态预测与动作解码有助于更好利用预测信息。
- 方法保持了基线推理模式，仅增加两个前缀token，部署开销极小。

### 7. 优点

- **方法轻量**：仅增加两个可学习查询token，无需修改骨干架构，推理时无需额外计算或未来生成。
- **明确动机驱动**：通过特权诊断实验首先确认未来观测的效用，再设计可部署方案，逻辑清晰。
- **双查询设计精巧**：分离未来预测与动作生成，允许动作查询注意力到预测状态，避免了表示冲突。
- **分层分析**：揭示长程场景的显著增益，验证了未来预测对长程导航的必要性。
- **实验公平**：基线与FSC-VLN共享所有训练配置，仅差分额外查询和对齐损失。

### 8. 不足与局限

- **未来目标来源**：使用专家轨迹的未来观测，与策略实际on-policy未来可能偏移，可能造成分布不匹配。
- **固定Δ值**：Δ=32步对所有场景和轨迹长度固定，可能不适合短轨迹或需要不同预测步长的场景。
- **单一骨干与任务**：仅在Qwen2-7B上验证，未探索其他VLM骨干（如LLaVA、InternVL）或其他导航环境（如REVERIE、CVDN、NLVR等）。
- **缺乏与其他方法的直接对比**：未与NaVid、Uni-NaVid、NaVILA等SOTA方法在标准VLN-CE评估集上比较整体性能，仅与自身基线对比。
- **超参数敏感性未知**：未消融λF、投影维度d'、投影结构等对性能的影响。
- **计算资源说明不完整**：未给出训练总时长、GPU型号，无法估计可复现性。
- **推理开销仅定性**：未定量测量FPS或平均推理时间，声称接近基线缺乏数据支持。

（完）
