---
title: "DSWAM: A Dual-System World Action Foundation Model for Fine-Grained Robot Manipulation"
title_zh: DSWAM：一种用于细粒度机器人操作的双系统世界动作基础模型
authors: "Jian Zhu, Jianjun Zhang, Taiyi Su, Tianbin Liu, Zhangyuan Wang, Kai Xie, Zitai Huang, Chong Ma, Youzhang He, Tianjian Wang, Hanyang Wang, Weihao Ding, Yi Xu"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04927v1"
tags: ["query:wmi"]
score: 9.0
evidence: 世界动作模型作为内部环境模型用于规划和操作
tldr: 世界动作模型虽具物理执行优势，但缺乏显式语言级规划接口，难以分解粗粒度指令。DSWAM提出双系统框架，保持System1 WAM执行器作为默认控制路径，仅在需要时激活System2视觉语言子任务规划器。通过TensorRT加速和异步执行实现实时控制，并在DeMaVLA设置下与VLA策略进行公平对比。该工作弥合了WAM的规划分解差距，为两类策略提供了可靠比较基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04927v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1712, \"height\": 887, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04927v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1583, \"height\": 2126, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04927v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1656, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04927v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1209, \"height\": 275, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04927v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1215, \"height\": 190, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04927v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1290, \"height\": 280, \"label\": \"Table\"}]"
motivation: 现有WAM缺乏语言级规划能力，无法分解复杂任务；且缺乏公平的WAM与VLA机器人对比设置。
method: DSWAM包含System1 WAM执行器（动作预测与视频联合训练）和System2 VLM子任务规划器，并集成TensorRT加速与异步执行。
result: 在DeMaVLA现实变形操作设置下，与VLA策略进行匹配实验，验证了双系统框架的有效性与公平性。
conclusion: DSWAM通过双系统设计实现粗粒度指令到细粒度子任务的分解，并为WAM与VLA的比较提供了标准化基准。
---

## 摘要
世界动作模型（WAMs）通过使用基于视频的世界建模作为机器人动作学习的密集监督，为视觉-语言-动作（VLA）策略提供了一种有前景的替代方案。现有的WAMs擅长基于物理的执行，但通常缺乏基于VLM的VLA中用于分解粗略指令的显式语言级规划接口。当家务任务涉及复杂的多步目标时，这种分解变得重要，因为粗略的用户命令需要转换为一系列细粒度的可执行子任务。同时，该领域仍然缺乏VLA和WAM执行能力的公平真实机器人比较，因为现有系统在数据、机器人载体和任务协议上往往不同。为了解决分解差距以及对受控WAM-VLA比较的需求，我们引入了DSWAM，一种用于细粒度机器人操作的双系统世界动作基础模型。DSWAM保持系统1 WAM执行器作为默认控制路径，并仅在任务分解有用时可选地激活系统2视觉-语言子任务规划器。规划器根据短期视觉历史和全局任务提示预测可执行的子任务，而WAM执行器为每个指令或子任务执行具有世界感知的动作生成。执行器通过动作预测和视频联合训练进行训练，但推理时直接预测动作块，无需显式的未来视频生成。为了使该执行路径在实际机器人上实用，我们进一步集成了TensorRT加速、异步执行和实时分块（RTC），使得策略查询不会阻塞机器人控制。为了与VLA策略进行公平的真实机器人比较，我们在DeMaVLA真实世界可变形操作设置下构建和评估DSWAM，使用匹配的机器人平台、预训练数据、后训练数据和评估标准。

## Abstract
World Action Models (WAMs) provide a promising alternative to Vision-Language-Action (VLA) policies by using video-based world modeling as dense supervision for robot action learning. Existing WAMs excel at physically grounded execution, but typically lack the explicit language-level planning interface in VLM-based VLAs for decomposing coarse instructions. Such decomposition becomes important when household tasks involve complex multi-step goals, where coarse user commands need to be converted into sequences of fine-grained executable subtasks. Meanwhile, the field still lacks a fair real-robot comparison between VLA and WAM execution capabilities, since existing systems often differ in data, robot embodiments, and task protocols. To address both the decomposition gap and the need for a controlled WAM-VLA comparison, we introduce DSWAM, a Dual-System World Action Foundation Model for fine-grained robot manipulation. DSWAM keeps a System 1 WAM executor as the default control path and optionally activates a System 2 vision-language subtask planner only when task decomposition is useful. The planner predicts executable subtasks from short-term visual history and a global task prompt, while the WAM executor performs world-aware action generation for each instruction or subtask. The executor is trained with action prediction and video co-training, but inference directly predicts action chunks without explicit future video generation. To make this execution path practical on real robots, we further integrate TensorRT acceleration, asynchronous execution, and real-time chunking (RTC) so that policy queries do not block robot control. To provide a fair real-robot comparison with VLA policies, we build and evaluate DSWAM under the DeMaVLA real-world deformable manipulation setting with matched robot platform, pretraining data, post-training data, and evaluation criteria.

---

## 论文详细总结（自动生成）

# DSWAM 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有世界动作模型（WAMs）通过视频建模提供密集的物理监督，在接触密集型操作任务中表现优异，但缺乏显式的语言级规划接口，难以将粗粒度指令（如“整理桌子上的物品”）分解为细粒度的可执行子任务序列。而基于VLM的VLA策略虽具有自然语言规划能力，但通常采用直接观测-动作映射，缺少世界建模的时域监督。此外，学术界缺乏在相同机器人平台、数据、任务协议下公平比较WAM与VLA执行能力的基准。
- **整体含义**：本文提出DSWAM双系统框架，解耦语义任务分解（系统2）与物理执行（系统1），并提供了在DeMaVLA设置下与VLA策略的受控对比，旨在同时弥补规划分解差距和比较基准缺失这两个空白。

## 2. 论文提出的方法论

### 2.1 核心思想
- **双系统设计**：系统1（WAM执行器）作为默认控制路径，直接处理原子指令或已可靠执行的任务；系统2（视觉-语言子任务规划器）作为可选的高层模块，仅在指令需要分解时激活，将粗指令转化为有序子任务序列。
- **解耦原则**：系统2负责语义任务组织，系统1负责世界感知的物理执行，避免对简单任务强制规划。

### 2.2 关键技术细节
- **系统2（子任务规划器）**：
  - 基于Rynnbrain4B风格的视觉-语言模型，输入为最近5帧（1 Hz采样）的短时视觉历史 \(X_t\) 和全局任务提示 \(p\)，输出下一个子任务指令 \(s_t = P_\phi(X_t, p)\)。
  - 训练数据通过预训练视觉-语言标注器对机器人轨迹视频进行子任务分割，构建过渡感知的监督标签：窗口在子任务内部时预测当前子任务，在非最终子任务边界处预测下一个子任务，在最终子任务窗口预测“done”。
  - 优化目标为自回归语言建模损失 \(L_{\text{plan}}\)。
  - 部署时，每2秒请求一次规划器，返回的子任务作为系统1的语言条件。

- **系统1（WAM执行器）**：
  - 编码多视图图像、语言指令/子任务、本体感知为潜在世界特征 \(z_t = f_\theta(I_t^{1:3}, \ell_t, q_t)\)，并直接预测动作块 \(A_t = [a_t, ..., a_{t+H-1}]\)，使用条件流匹配生成连续动作。
  - 训练时有双重损失：动作流匹配损失 \(L_{\text{act}} = L_{\text{FM}}(A_t)\) 和视频联合训练损失 \(L_{\text{vid}} = L_{\text{FM}}(V_{t+1:t+T})\)，总损失 \(L = L_{\text{act}} + \lambda_{\text{vid}} L_{\text{vid}}\)。视频损失提供世界建模监督，但动作分支不利用未来视觉作为特权信息。
  - 推理时直接输出动作块，不进行显式未来视频生成，结合TensorRT加速和异步实时分块（RTC）实现低延迟控制。

### 2.3 流程说明
- 训练阶段：系统2使用轨迹视频生成过渡感知标签训练；系统1使用真实机器人数据联合训练动作预测和视频预测。
- 推理阶段：若指令为原子指令或任务可靠，系统1直接处理；若为粗指令，系统2每2秒输出子任务，系统1据此执行动作块。异步执行使得策略查询不阻塞机器人控制环。

## 3. 实验设计

### 3.1 数据集与场景
- **RoboTwin 2.0 仿真基准**：包含50个双臂操作任务，分为clean（固定初始配置）和randomized（随机物体位姿和场景布局）两种设置。
- **DeMaVLA 真实世界折叠协议**：使用ALOHA风格双臂平台，评估衬衫、裙子、裤子、毛巾四类可变形物体，每类两个物理实例（easy和hard），每个实例10次试验，共80次试验。任务流程包括从篮子中取物、放置、展平、折叠。
- **系统2排序任务**：真实桌面排序任务，指令“整理桌子上的物品”，分解为两个子任务（捡起玩具车放入盒子、捡起面包放在盘子上），并随机化初始物体配置。

### 3.2 对比方法
- **仿真**：π0, π0.5, DeMaVLA, Motus, Fast-WAM 作为基线。
- **真实折叠**：π0, DeMaVLA（DSWAM在WAM-only模式下对比，系统2禁用以保证执行能力比较公平）。
- **系统2研究**：对比使用原始粗指令训练的执行器与使用子任务级指令训练的执行器。

### 3.3 评估指标
- **RoboTwin 2.0**：成功率（SR）。
- **真实折叠**：成功率、平均完成时间（失败试次按超时时间计）。
- **系统2排序**：成功率、每次推演的平均执行错误次数。

## 4. 资源与算力

- 论文**未明确说明**训练所使用的GPU型号、数量、训练时长等具体算力信息。仅提及在推理阶段使用NVIDIA GeForce RTX 5090进行TensorRT加速（CUDA 12.9，TensorRT 10.16.1），且系统2基于Rynnbrain4B-style模型。
- 但可以推断：大规模预训练（5000+小时真实机器人数据，视频模型采用Wan2.2-TI2V-5B等）需要大量GPU资源，然而原文未披露。

## 5. 实验数量与充分性

### 5.1 实验组数
- **仿真基准**：50个任务的clean和randomized设置，共100个成功率数据点，对比5种方法（包括DSWAM）。
- **真实折叠**：4个类别，每类2个实例×10次=20次试验，共80次试验，对比3种方法。
- **系统2研究**：2种训练步数（6000/18000），2种指令类型（原始/子任务级），共4种条件，每种条件给出成功率和错误次数。
- **效率实验**：TensorRT延迟测试（PyTorch vs TensorRT BF16），以及异步RTC在easy实例上的滚动测试（衬衫、裤子各10次）。

### 5.2 充分性与公平性
- **优点**：真实世界折叠实验严格遵循DeMaVLA协议，匹配机器人平台、预训练数据、后训练数据、任务协议和成功标准，使WAM与VLA的比较不受混杂因素干扰。仿真涵盖50个多样化任务，评估了泛化性。
- **不足**：
  - 折叠实验仅涉及可变形操作（衣物），未覆盖刚性物体、装配、精细操作等场景。
  - 系统2研究仅在一个排序任务上验证，子任务边界监督的通用性有待更多复杂任务验证。
  - 未进行系统1独立消融（如去掉视频联合训练的影响、不同λ_vid的影响）。
  - 未对比其他双系统方案（如直接使用VLM+WAM拼接），也未分析系统2规划错误对执行的级联影响。
  - 仅在单个机器人平台（ALOHA风格）上验证，跨本体泛化未测试。

## 6. 论文的主要结论与发现

1. **双系统框架有效**：DSWAM在WAM-only模式下，真实折叠成功率从DeMaVLA的92.5%提升至96.3%，完成时间从2分18秒缩短至1分44秒，证明了WAM执行器在可变形操作中的优势。
2. **仿真泛化性**：在RoboTwin 2.0上达到clean 92.38%和randomized 91.90%的平均成功率，优于所有基线（包括Fast-WAM：91.88%/91.78%）。
3. **系统2提升稳定性**：子任务级监督在排序任务上将成功率从75.7%提升至100.0%，错误次数从3.53降至0.65，证明在需要分解的任务中显式规划有益。
4. **部署可行性**：TensorRT BF16加速实现2.69倍端到端延迟降低（198.2 ms→73.8 ms），异步RTC进一步减少控制等待，在easy实例上实现100%成功率并缩短完成时间。

## 7. 优点

1. **方法设计创新**：首次提出WAM的双系统解耦架构，保留WAM物理执行强项的同时，引入可选的高层语义规划，避免了对简单任务的过度规划。
2. **公平对比贡献**：建立了在相同平台、数据、协议下比较WAM与VLA的标准化基准，为领域提供可靠参考。
3. **实用化导向**：推理时不生成未来视频，结合TensorRT和异步RTC，使大型模型能够实时运行，解决了WAM部署的延迟瓶颈。
4. **丰富实验**：仿真+真实+消融+效率，从多个维度验证方法有效性。

## 8. 不足与局限

1. **实验场景覆盖有限**：真实操作仅测试可变形物体折叠，未涉及刚性物体操作、装配、移动等更广泛任务；系统2实验仅一个排序场景。
2. **可重复性不足**：未公开训练资源细节（GPU数量、训练时长），难以复现大规模预训练过程。
3. **消融不完整**：未单独分析视频联合训练的贡献大小（如λ_vid取不同值的效果）、未对比不使用联合训练时的性能；系统2的触发阈值（何时需要分解）未明确研究。
4. **系统2规划鲁棒性未知**：未分析规划器输出错误子任务时的恢复机制或失败情况对整体性能的影响。
5. **跨平台泛化缺失**：仅在ALOHA双臂平台上验证，未测试单臂、移动操作或其他形态的机器人。
6. **长期任务表现未评估**：时间限制和成功标准可能偏向短周期任务，对于需要数十步的长程任务，系统1的误差累积和系统2的规划频次有待验证。

（完）
