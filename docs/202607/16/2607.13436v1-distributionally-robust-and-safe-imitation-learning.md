---
title: Distributionally Robust and Safe Imitation Learning
title_zh: 分布鲁棒且安全的模仿学习
authors: "Ahmed Aboudonia, Naira Hovakimyan"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13436v1"
tags: ["query:wmi"]
score: 9.0
evidence: 处理分布偏移和安全性问题的模仿学习框架
tldr: 模仿学习对分布偏移敏感，可能导致不安全行为。本文提出分布鲁棒且安全的模仿学习框架，结合Taylor Series Imitation Learning (TaSIL)处理策略诱导偏移，并通过分布鲁棒自适应控制应对不确定性偏移。在无人机案例中，该方法使无人机在不确定环境下避开危险区域完成任务。该框架统一处理两种偏移，提升了模仿学习的安全性和鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13436v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1455, \"height\": 702, \"label\": \"Figure\"}]"
motivation: 模仿学习在分布偏移下性能下降，存在安全风险，需同时考虑策略诱导和不确定性两种偏移。
method: 提出分布鲁棒且安全的模仿学习框架，融合TaSIL和分布鲁棒自适应控制，优化性能并满足安全约束。
result: 在无人机案例中，该方法成功使无人机在不确定环境中避开不安全区域完成任务。
conclusion: 该框架有效处理分布偏移，提升了模仿学习的安全性和鲁棒性。
---

## 摘要
模仿学习在复杂决策任务中取得了显著成功。然而，其性能对分布偏移高度敏感，这可能导致重大安全风险。我们提出了一种分布鲁棒且安全的模仿学习框架，明确解决了策略诱导和不确定性诱导的分布偏移。该方法利用泰勒级数模仿学习来缓解策略诱导的偏移，并采用分布鲁棒自适应控制来处理不确定性诱导的偏移，从而构建了一个统一框架。该架构使得在分布不确定性下优化性能的同时，系统地考虑安全约束的模仿学习问题成为可能。我们通过在无人机案例研究中展示了所提方法的有效性，其中无人机在不确定环境中执行任务，同时避免不安全区域。

## Abstract
Imitation learning (IL) has achieved remarkable success in complex decision-making tasks. However, its performance is highly sensitive to distribution shifts, which can pose significant safety risks. We propose a distributionally robust and safe IL framework that explicitly addresses both policy-induced and uncertainty-induced distribution shifts. Our approach develops a unified framework leveraging Taylor Series Imitation Learning (TaSIL) to mitigate policy-induced shifts and distributionally robust adaptive control to handle uncertainty-induced shifts. This architecture enables the formulation of an IL problem that optimizes performance under distributional uncertainty while systematically accounting for safety constraints. We demonstrate the effectiveness of the proposed approach on an unmanned aerial vehicle (UAV) case study where the UAV performs a task in an uncertain environment while avoiding unsafe regions.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：模仿学习（IL）在实际部署中面临严重的**分布偏移**问题，主要源自两类：  
  - **策略诱导偏移**：学习策略的微小误差随时间累积，导致轨迹偏离专家。  
  - **不确定性诱导偏移**：环境中存在的**认知不确定性**（建模误差）和**随机不确定性**（过程噪声）同样会导致性能退化。  
- **现实风险**：这两种偏移可能引发**不安全行为**，尤其在无人机等安全关键系统中。  
- **论文目标**：提出一个能够**同时鲁棒处理两类分布偏移**，并**显式考虑安全性**的统一模仿学习框架。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **总体架构**：采用分层控制思想，融合两个模块：  
  - **TaSIL（泰勒级数模仿学习）**：用于缓解**策略诱导分布偏移**。通过最小化学习策略与专家策略及其高阶导数之间的差异（而非仅输出差），使策略在状态空间内局部对齐，增强稳定性。  
  - **L₁-DRAC（L₁分布鲁棒自适应控制）**：用于缓解**不确定性诱导分布偏移**。通过在线自适应估计并补偿系统不确定性，将真实系统状态分布与名义系统状态分布之间的Wasserstein距离控制在有界范围内。  
- **核心技术创新**：  
  - **分布鲁棒TaSIL目标**：将标准TaSIL的损失函数改写为**分布鲁棒形式**，在专家轨迹周围定义一个半径为ρ₁的Wasserstein球，从中采样最坏情况下的状态分布来训练策略，保证在不确定性下性能不过度恶化。  
  - **安全约束集成**：引入**条件风险价值（CVaR）** 近似机会约束，并使用ReLU函数将违反安全约束的惩罚项加入损失函数，使学习策略能在必要时主动偏离专家以保证安全性。  
  - **最终损失函数**：`loss = 分布鲁棒TaSIL项 + λ·ReLU(安全约束项)` 示例公式见论文(13)-(15)。

### 3. 实验设计：数据集/场景、基准、对比方法

- **场景**：无人机（UAV）仿真，状态包含位置、线速度、欧拉角、角速度共12维，控制输入为4个（推力、滚转/俯仰/偏航力矩）。任务：模仿专家规划器生成圆轨迹（半径0.8）增量，同时需避开不安全区域（x>0.8或y>0.8凸区域）。  
- **数据集**：由专家规划器在名义（无不确定性）环境中生成**10条轨迹**，每条150步。初始状态在轨迹附近随机采样。  
- **对比方法**（四种情况）：  
  1. 标准TaSIL（无不确定性）  
  2. 标准TaSIL（有不确定性，即添加认知和随机噪声）  
  3. 标准TaSIL + L₁-DRAC（有不确定性）  
  4. **本文提出的分布鲁棒且安全TaSIL + L₁-DRAC**（有不确定性）  
- **评价指标**：定性蒙特卡洛仿真轨迹图，观察是否跟踪圆轨迹以及是否避开不安全区域。未提供定量指标（如成功率、距离误差等）。

### 4. 资源与算力

- **文中未明确说明**使用的GPU型号、数量、训练时长等算力信息。  
- 仅提及使用**Euler-Maruyama数值积分**进行仿真（采样时间0.01s），策略为两层32节点GELU网络，训练1000轮。  
- 推测算力需求较低，个人GPU可完成。

### 5. 实验数量与充分性

- **实验数量**：仅有一组蒙特卡洛仿真（每种条件多次初始条件，但未说明具体次数）。未进行**消融研究**（如单独验证分布鲁棒项或安全项的影响）、**不同不确定水平下的鲁棒性对比**、**与其他鲁棒IL方法**（如DAgger+DART、DRIP等）的定量对比。  
- **充分性评价**：作为概念验证，定性展示了核心效果；但实验不够充分：  
  - 缺乏定量指标（成功率、平均距离、安全违反次数）。  
  - 仅测试一个任务（圆轨迹），通用性未验证。  
  - 未与现有基线（如加噪声的BC、DAgger、DRIP等）直接对比。  
  - 参数（ρ₁、λ等）如何选择未给出系统分析。  
- **公平性**：对比方法中的标准TaSIL未添加任何安全处理，对安全指标上不公平；但论文目的本身就是提出安全框架，因此合理。

### 6. 论文的主要结论与发现

- **主要结论**：  
  - 提出的分布鲁棒且安全仿学习框架能**同时处理策略诱导和不确定性诱导分布偏移**。  
  - 在无人机仿真中，该框架**既能有效模仿专家轨迹**，又能**在接近不安全区域时主动偏离**以保证安全。  
  - L₁-DRAC可提供鲁棒性保证，而分布鲁棒TaSIL训练使政策对不确定性具有隐式泛化能力。  
- 标准TaSIL在不确定性下性能退化，添加L₁-DRAC后可部分恢复，但若不处理安全性仍会进入危险区域；本文方法弥补了该缺陷。

### 7. 优点

- **统一框架**：首次将策略诱导和不确定性诱导分布偏移放在一个框架中解决，并显式集成安全约束。  
- **理论支撑**：基于Wasserstein距离的分布鲁棒优化、L₁自适应控制的鲁棒性证明、以及泰勒级数局部对齐的原理，方法具有理论基础。  
- **可分离设计**：TaSIL处理策略诱导问题、L₁-DRAC处理不确定性、安全项可独立加入，具有模块化的好处。  
- **仿真结果直观**：四张子图清晰说明各模块的效果，特别是最后一张图显示无人机在不安全区域外绕行。

### 8. 不足与局限

- **实验覆盖有限**：仅一个UAV仿真场景，未在真实机器人或更复杂环境验证。  
- **缺乏定量基准**：未使用标准IL基准（如MuJoCo、CARLA等），也未与DAgger、GAIL、DRIP等方法定量对比。  
- **参数依赖性**：Wasserstein球半径ρ₁、安全惩罚权重λ等核心超参数需要经验调节，文中未给出调参指南或灵敏度分析。  
- **假设局限性**：假设专家策略及其Jacobian可用（可能需要额外的系统辨识或可微专家）；安全区域为凸多面体形式，非凸区域未考虑。  
- **可扩展性**：网络设计简单，未在高维状态空间（如视觉输入）测试。  
- **未公开代码**：论文未附代码或数据集，可重复性存疑。

（完）
