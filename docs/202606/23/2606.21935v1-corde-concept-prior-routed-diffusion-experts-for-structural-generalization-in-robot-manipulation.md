---
title: "CoRDE: Concept-Prior Routed Diffusion Experts for Structural Generalization in Robot Manipulation"
title_zh: "CoRDE: 概念先验路由扩散专家用于机器人操作中的结构泛化"
authors: "Haidong Huang, Xixin Zhao, Yaohua Zhou, Jiayu Song, Jiayi Zhang, Jun Ma, Haiyue Zhu, Xiaocong Li"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21935v1"
tags: ["query:wmi"]
score: 9.0
evidence: 扩散模型用于机器人模仿学习，实现结构泛化
tldr: 扩散模型在机器人多任务长程操作中因结构泛化不足产生梯度冲突，纯数据驱动的MoE方法易路由崩溃且参数爆炸。CoRDE提出概念先验路由的扩散专家框架，通过冻结概念编码器提取语义分布，利用软映射矩阵引导变分后验责任，并引入熵控制实现可靠路由与行为多样性平衡，同时采用LoRA构建参数高效专家池。实验证明CoRDE系统减少路由崩溃，形成鲁棒语义对齐的专家分配，动作质量和增量学习效率显著优于现有基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21935v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1653, \"height\": 871, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21935v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1860, \"height\": 942, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21935v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 902, \"height\": 379, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21935v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1746, \"height\": 403, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21935v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 661, \"height\": 181, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21935v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1485, \"height\": 372, \"label\": \"Table\"}]"
motivation: 现有扩散模型在多任务长程机器人操作中缺乏结构化泛化能力，MoE方法存在路由崩溃和参数膨胀问题。
method: CoRDE利用冻结概念编码器提取语义先验，通过软映射矩阵和熵控制引导专家路由，并采用LoRA参数高效专家池。
result: CoRDE显著减少路由崩溃，形成语义一致专家分配，动作质量和增量学习效率均超越基线方法。
conclusion: CoRDE为机器人操作的结构化泛化提供了语义引导且参数高效的扩散专家框架。
---

## 摘要
扩散模型在机器人模仿学习中擅长捕捉多模态动作分布。然而，在多任务和长视界场景中，单体架构缺乏结构泛化能力，会因不同语义子阶段之间的梯度冲突而受限。纯数据驱动的混合专家（MoE）方法虽然引入了分工，但经常触发路由崩溃，并且实例化全尺寸专家会导致参数爆炸和高扩展成本。为了解决这些问题，我们提出概念先验路由扩散专家（CoRDE），一种结构引导的变分蒸馏框架。CoRDE从冻结的概念编码器中提取语义分布，通过可学习的软映射矩阵指导变分后验责任。该机制引入了一个熵控制的责任推理过程，在可靠的语义预测下鼓励确定性的路由，同时保留随机扩散项以保持行为多样性。为了克服参数膨胀，CoRDE采用参数高效的专家池，在共享的冻结主干上使用低秩自适应（LoRA）。理论分析表明，混合分数差异受责任加权局部专家误差的约束，支持在低秩专家自适应下的高保真生成。实证评估证实，与现有基线相比，CoRDE系统地减少了路由崩溃，形成了稳健的、语义对齐的专家分配，同时实现了更优的动作质量和增量学习效率。

## Abstract
Diffusion models excel at capturing multi-modal action distributions in robot imitation learning. However, in multi-task and long-horizon scenarios, monolithic architectures lack structural generalization capabilities, suffering from gradient conflicts between distinct semantic sub-stages. While pure data-driven Mixture-of-Experts (MoE) methods introduce labor division, they frequently trigger routing collapse, and instantiating full-scale experts causes parameter explosion and high expansion costs. To address these issues, we propose Concept-prior Routed Diffusion Experts (CoRDE), a structure-guided variational distillation framework. CoRDE extracts semantic distributions from a frozen concept encoder to guide the variational posterior responsibility via a learnable soft mapping matrix. This mechanism introduces an entropy-controlled responsibility inference process that encourages confident routing under reliable semantic predictions while preserving the stochastic diffusion term for behavioral diversity. To overcome parameter inflation, CoRDE employs a parameter-efficient expert pool using Low-Rank Adaptation (LoRA) on a shared frozen backbone. Theoretical analysis shows that the mixture score discrepancy is bounded by responsibility-weighted local expert errors, supporting high-fidelity generation under low-rank expert adaptation. Empirical evaluations confirm that, compared to existing baselines, CoRDE systematically reduces routing collapse, forming robust, semantically aligned expert allocations while achieving superior action quality and incremental learning efficiency.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：扩散模型在机器人多任务、长视界模仿学习中虽然能捕捉多模态动作分布，但采用单一全局参数的单体架构在面对不同语义子阶段（如低频空间导航 vs. 高频精细接触）时会产生严重的**梯度冲突**和**灾难性干扰**，缺乏**结构泛化**能力。
- **现有方法局限**：
  - 纯数据驱动的混合专家（MoE）方法引入分工，但依赖数据似然的路由机制频繁触发**路由崩溃**（winner-takes-all），强行加入负载均衡损失又扭曲实际物理任务的不均匀分布。
  - 实例化全尺寸专家导致**参数爆炸**和扩展成本过高。
  - 现有专家分配缺乏可解释的语义边界，易产生路由抖动和动作混淆。
- **论文目标**：提出 CoRDE，将高层语义概念先验与扩散 MoE 的生成能力统一，实现稳定、可解释、参数高效的专家路由，提升多任务长视界操作的结构泛化能力。

## 2. 方法论

### 2.1 核心思想
- 利用**冻结的概念编码器**从多模态观测中提取稳定的语义分布 \(p_\phi(c|o)\)。
- 通过**可学习的软映射矩阵** \(P_{CE}(z|c)\) 将概念映射到专家先验 \(p_{\text{struct}}(z|o) = \sum_c p_\phi(c|o)P_{CE}(z|c)\)。
- 结合结构先验与行为证据（基于教师分数场的局部拟合精度），推导出**变分责任后验** \(q^*(z|o,a_0)\)，形式为几何平均：
  \[
  q^*(z|o,a_0) = \frac{1}{Z} \bar{r}(z|o,a_0)^{\frac{1}{1+\lambda(o)}} p_{\text{struct}}(z|o)^{\frac{\lambda(o)}{1+\lambda(o)}}
  \]
  其中 \(\lambda(o)\) 根据概念置信度自适应调整。
- 专家池采用**LoRA** 对共享冻结主干进行低秩适应，避免参数爆炸。
- 理论证明：混合分数场的 L2 误差受各专家责任加权局部误差上界约束（定理 2），且纯低秩更新不破坏行为多样性，因为 SDE 的随机扩散项始终注入全秩噪声（Remark）。

### 2.2 关键技术细节
- **结构引导的责任推理**：优化结构正则化变分目标（公式 4），推导出闭式最优后验，实现**双熵动态**——宏观路由熵最小化（确定性）与微观扩散熵最大化（多样性）。
- **路由器解耦**：路由器 \(q_\psi(z|o)\) 仅通过 KL 散度拟合后验 \(q^*\)，避免下游分数匹配任务的梯度冲突。
- **EM 式软对齐**：利用 Dirichlet 平滑和指数移动平均更新软映射矩阵 \(P_{CE}\)，建立概念-专家间的可解释多对多映射。
- **训练与推理两阶段**：
  - 训练：离线蒸馏，含 E-step（推断后验）和 M-step（更新 LoRA 专家、路由器、映射矩阵）。
  - 推理：在线执行，通过状态条件路由激活 top-k 专家，利用混合分数场进行 SDE 去噪生成。

## 3. 实验设计

- **数据集与场景**：
  - **LIBERO**：包含四个标准套件（L-Spatial, L-Object, L-Goal, L-Long），用于评估长视界多任务操作的**成功率**。
  - **D3IL**：包含 7 个任务（Avoiding, Aligning, Pushing, Stacking-1, Stacking-2, Sorting (Image), Stacking (Image)），提供高度多模态的人类演示，用于评估**动作多样性（Task Entropy）** 和**推理效率**。
- **对比方法**：
  - 单体：Diffusion Policy (Teacher)
  - 蒸馏 MoE：VDD (Distill-MoE，证据-only，无结构先验)
  - CoRDE 两个变体：使用 HiMaCon 编码器 / AutoCGP 编码器（均无人工标注）
- **实验公平性**：所有蒸馏学生方法（Distill-MoE 和 CoRDE）共享**相同参数预算、架构、专家数量、LoRA 配置和训练步数**，唯一变量是结构先验的集成。
- **评估指标**：
  - LIBERO：任务成功率
  - D3IL：成功率 + 任务熵（0~1 范围分类指标）+ 推理时间（毫秒）/ NFE
  - 路由诊断：\(H_{\text{route}}\), \(H_{\text{post}}\), 困惑度 PPL

## 4. 资源与算力

- **未明确说明**：论文中**没有提及**使用的 GPU 型号、数量、训练时长或总计算量。仅在实验设置中提到“随机种子多次”和“训练协议与主实验一致”，但无具体算力细节。

## 5. 实验数量与充分性

- **主实验**：
  - LIBERO：4 个套件 × 多个随机种子，报告成功率 + 宏平均。
  - D3IL：7 个任务 × 多个随机种子，报告成功率 + 任务熵 + 推理时间。
- **对比组**：包括老师（Diffusion Policy）、证据-only MoE、CoRDE（HiMaCon）、CoRDE（AutoCGP），覆盖了有无语义先导和不同概念编码器。
- **消融实验**（表 III）：去除 PCE（保持恒等映射）、固定 λ、证据-only、路由器无责任监督，共 4 组变体，分别评估 LIBERO 成功率、D3IL 成功率/熵、路由诊断指标。
- **充分性评价**：
  - 实验覆盖了**性能、多样性、效率**三个维度。
  - **公平性控制严格**：参数预算、架构、训练步数完全一致，排除了容量偏差。
  - **路由诊断**量化了路由崩溃与否，增强了结论可靠性。
  - 不足：缺少**真实机器人部署**实验；缺少更多样化的真实世界基准；未报告统计显著性检验；消融实验的变体数量较少（但覆盖了关键设计点）。

## 6. 主要结论与发现

- **性能**：CoRDE 在 LIBERO 所有套件上**一致超越**单体 Diffusion Policy 和证据-only Distill-MoE，尤其在语义结构最强的 L-Long 和 L-Goal 上提升最显著。
- **路由机制**：CoRDE 的**路由熵低**且**专家困惑度高**（PPL>1），表明形成了语义对齐的确定性分配，而非崩溃。消融实验显示去除非关键组件（如 PCE、自适应 λ）后路由质量下降。
- **多样性保持**：在 D3IL 上，CoRDE 的 **Task Entropy 接近老师**（DP），远高于证据-only MoE 和固定 λ 变体，验证了随机扩散项（SDE）保留全秩多样性的理论。
- **推理效率**：CoRDE 实现单步生成（NFE=1），推理时间 2.63 ms，比老师（55.62 ms）快一个数量级，与 VDD 相当但参数更少。
- **参数效率**：LoRA 专家池有效避免了参数爆炸，且理论保证混合分数误差有界，实际成功率不降反升。

## 7. 优点

- **方法创新性**：
  - 首次将**语义概念先验**与**变分蒸馏**融合，引导 MoE 路由，从原理上消除路由崩溃。
  - **软映射矩阵 + EM 自组织更新**实现概念-专家的可解释多对多映射，无需人工标签。
  - **LoRA 参数高效专家池**结合**随机扩散项理论保证**，兼顾低秩适应与全秩多样性。
- **理论贡献**：
  - 严格推导最优后验的闭式解（几何平均）与路由熵界。
  - 证明混合分数 L2 误差受责任加权局部误差约束，为低秩专家生成高保真动作提供理论支撑。
  - 明确阐述 LoRA + SDE 分离机制如何保留多样性。
- **实验设计**：
  - 严格控制参数预算，确保性能提升源于结构先导而非容量增加。
  - 路由诊断指标（H_route、H_post、PPL）直观量化路由稳定性。
  - 同时评估成功率和多样性，避免片面优化。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅基于仿真 benchmark（LIBERO、D3IL），**未在真实机器人平台**验证，泛化性到实际物理世界尚存疑。
  - 缺少长视界任务的**成功率 vs. 推理时间权衡**的深入分析（如不同 NFE 下的表现）。
  - 未与其他 MoE 蒸馏方法（如 SMP、ForceVLA）直接比较，尽管它们任务定义不同。
- **概念编码器依赖**：需要预先训练好的概念编码器（HiMaCon/AutoCGP），其本身可能引入偏差或在高噪声观测下不准确。
- **计算资源未报告**：使得可复现性和部署成本估算困难。
- **潜在局限**：
  - 自适应 \(\lambda(o)\) 依赖概念置信度，若概念编码器在罕见任务上不准确，结构先导可能误导路由。
  - 专家数量 K 和 LoRA 秩 r 的选择未做系统分析，可能影响泛化上限。
  - 未探讨开放词汇概念（如语言指令）的扩展性，仅用自监督视觉概念。

（完）
