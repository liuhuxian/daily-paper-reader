---
title: "MiniWorld: Democratizing the Training of Video World Models from Scratch"
title_zh: "MiniWorld: 从零开始普及视频世界模型训练"
authors: "Yian Zhao, Ruochong Zheng, Hongcan Guo, Yu Yan, Jian Zhang, Jie Chen"
date: 2026-08-02
pdf: "https://arxiv.org/pdf/2608.01127v1"
tags: ["query:wmi"]
score: 9.0
evidence: 从零训练视频世界模型，支持具身AI
tldr: 现有视频世界模型多依赖预训练生成模型微调，训练管线复杂且算力要求高，存在预训练与流式推理不一致。本文提出MiniWorld，一种从零训练流式视频世界模型的可复现框架：采用块因果Video Diffusion Transformer与Flow Matching，在Video VAE潜空间训练，配合分块非递减噪声调度和两阶段训练。在单台8-GPU服务器上数天即可训完，推理时通过滚动KV缓存和异步去噪实现高效流式生成。该框架轻量透明、完全可复现，为视频世界建模提供了开放基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.01127v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1704, \"height\": 1045, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01127v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1719, \"height\": 1060, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01127v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1721, \"height\": 930, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01127v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 842, \"height\": 450, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01127v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 847, \"height\": 406, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.01127v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1172, \"height\": 369, \"label\": \"Table\"}]"
motivation: 现有视频世界模型多依赖预训练生成模型微调，训练管线复杂、算力门槛高，且存在预训练与流式推理不一致的问题。
method: 方法上，MiniWorld采用块因果Video Diffusion Transformer与Flow Matching，在Video VAE潜空间从零训练，并配合分块非递减噪声调度和两阶段持续训练。
result: 在单台8-GPU服务器上数天内训练完成，推理时结合滚动KV缓存和异步去噪实现高效流式生成。
conclusion: MiniWorld提供轻量透明、完全可复现的基线，显著降低算力门槛，促进视频世界建模研究。
---

## 摘要
视频世界模型根据历史观测和控制信号预测未来观测，通过自回归状态转换实现长时程生成。与主要捕捉视觉外观和运动的常规视频生成模型不同，视频世界模型学习智能体动作下环境演化的底层动力学，为具身AI和交互式模拟提供基础。近期进展主要依赖于通过后训练或蒸馏来适配预训练的视频生成模型。虽然有效，但这些方法通常需要复杂的训练流程、大量的计算资源，并且存在双向预训练与因果流式推理之间的不匹配问题。近期研究表明，从零开始训练自回归视频世界模型是可行的且可扩展的。然而，社区仍然缺乏一个轻量级、透明且完全可复现的基线，能够用适度的计算资源端到端训练。我们提出了MiniWorld，一个从零开始训练流式视频世界模型的可复现框架。MiniWorld采用一个块因果视频扩散Transformer，在预训练Video VAE的潜空间中用流匹配进行训练。基于Diffusion Forcing，它采用分块非递减噪声调度和两阶段持续训练以改善时间建模和稳定性。在推理时，MiniWorld结合滚动KV缓存与流水线异步去噪，在有限计算下实现高效流式生成。整个模型可在单个8-GPU服务器上几天内训练完成。通过发布训练和推理代码库以及预训练检查点，我们希望MiniWorld能促进视频世界建模的未来研究。

## Abstract
Video world models predict future observations conditioned on historical observations and control signals, enabling long-horizon generation through autoregressive state transitions. Unlike conventional video generation models that primarily capture visual appearance and motion, video world models learn the underlying dynamics governing environment evolution under agent actions, providing a foundation for embodied AI and interactive simulation. Recent progress has largely relied on adapting pretrained video generation models through post-training or distillation. Although effective, these approaches often require complex training pipelines, substantial computational resources, and suffer from the mismatch between bidirectional pretraining and causal streaming inference. Recent studies have shown that training autoregressive video world models from scratch is feasible and scalable. However, the community still lacks a lightweight, transparent, and fully reproducible baseline trainable end-to-end with modest computational resources. We present MiniWorld, a reproducible framework for training streaming video world models from scratch. MiniWorld employs a block-causal Video Diffusion Transformer trained with Flow Matching in the latent space of a pretrained Video VAE. Building on Diffusion Forcing, it adopts a chunk-wise non-decreasing noise schedule and two-stage continued training to improve temporal modeling and stability. During inference, MiniWorld combines a rolling KV cache with pipelined asynchronous denoising for efficient streaming generation under bounded computation. The entire model can be trained within several days on a single 8-GPU server. By releasing the training and inference codebase and pretrained checkpoints, we hope MiniWorld will facilitate future research on video world modeling.

---

## 论文详细总结（自动生成）

# MiniWorld：从零开始训练视频世界模型的可复现框架——论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：训练视频世界模型（Video World Models）的算力门槛过高，社区缺乏一个轻量级、透明、完全可复现的**从零训练（from scratch）**基线。
- **现有方法的局限性**：
  - 主流范式是**适配预训练双向视频生成模型**（如Wan、Sora等）进行后训练/蒸馏，需要复杂的多阶段优化流程和大量计算资源。
  - 更根本的问题在于**结构性不匹配**：双向预训练与因果流式推理在训练目标上存在本质冲突，且训练时使用真实视频作为历史上下文，推理时却用模型自身生成的结果，导致**曝光偏差（exposure bias）**和误差累积。
  - 固定窗口续写策略会逐渐丢弃早期观测信息，导致长时程生成中出现时间漂移（temporal drift）和长程一致性退化。
- **研究意义**：视频世界模型是具身智能（embodied AI）和交互式模拟的关键构建模块。若能以较低算力从零训练流式世界模型，将显著降低该领域的研究门槛，推动长期因果记忆、错误累积、因果表征学习等前沿问题的研究。

## 2. 论文提出的方法论：核心思想、关键技术细节与算法流程

### 核心思想
MiniWorld 直接优化**动作条件化的下一状态预测（next-state prediction）**目标，而非适配双向扩散模型。通过块因果注意力、分块噪声调度和滚动KV缓存的联合设计，实现训练与推理目标的一致性，支持长时程流式生成。

### 关键技术组件

**（1）架构：块因果 Video Diffusion Transformer（Video DiT）**

- 单流Video DiT + 3D Patch Embedding，在预训练 Wan2.2 Video VAE 的潜空间中建模（时间压缩4×、空间压缩16×、48通道）。
- 采用**块因果自注意力**：块内 token 双向注意力，块间严格因果注意力（只能关注前序块）。
- 动作条件化：通过**AdaLN-LoRA调制机制**实现——共享调制网络 + 每层零初始化的低秩残差，解耦语义条件和特征调制。
- 支持模型规模从 0.12B 到 3B 的统一架构族。

**（2）训练目标：Rectified Flow + 分块非递减噪声调度**

- 使用 Rectified Flow 构建线性插值：
  \[
  z_\tau = (1-\tau)x + \tau\epsilon, \quad \tau\in[0,1]
  \]
  训练模型预测速度场 \( v^*(x,\epsilon) = x - \epsilon \)。
- 受 Diffusion Forcing 启发，为不同潜变量块分配独立扩散时间步，并施加**非递减约束**：
  \[
  \tau_1 \le \tau_2 \le \cdots \le \tau_M
  \]
  即早期块总是比后期块更干净，反映推理时“过去已收敛、未来仍不确定”的实际状态。
- 提出**Chunk-oriented Probability Propagation（CoPP）调度器**——随机选取锚定块，从锚点向外传播时间步，保持FoPP的均衡采样性质，同时覆盖多样化的有效轨迹。
- 损失函数仅作用于预测块集合 \( \Omega \)：
  \[
  \mathcal{L}_{FM} = \frac{1}{|\Omega|}\sum_{n\in\Omega} \left\| [v_\theta(z_\tau,\tau,c)]_n - (x_n - \epsilon_n) \right\|_2^2
  \]
- **两种上下文条件模式随机混合**：图生视频模式（仅首帧干净）和视频生视频模式（整个首块干净），统一训练框架下同时支持两种推理设定。

**（3）两阶段持续训练**

- 第一阶段（预训练）：短片段 21/46帧，快速学习局部动作条件动力学。
- 第二阶段（长视频持续训练）：125/253帧，暴露更长的因果上下文、动作轨迹和相机轨迹。
- 第二阶段使用 **SD3-style timestep shifting**：
  \[
  \tilde{\tau} = \frac{s\tau}{1+(s-1)\tau}
  \]
  重新分配时间步采样密度，优化长序列训练。

**（4）流式推理**

- **滚动KV缓存**：完成的块被提交至固定容量FIFO缓存，最早块被驱逐；首个干净上下文作为"锚定"（sink anchor）永久保留。活动去噪窗口保持有界计算成本。
- **RoPE重移位**：每次缓存更新后，从保留键中减去被驱逐块引入的时间偏移，避免位置编码超出训练范围的外推。
- **流水线异步去噪**：多个块可同时处于不同扩散时间步并行去噪，在线自适应分配 \( \Delta\tau \)，提供吞吐量与质量间的灵活权衡。

## 3. 实验设计：数据集、Benchmark 与对比方法

### 数据集
| 数据集 | 控制模态 | 评估目标 | 条件表示 |
|--------|---------|---------|---------|
| DROID | 7维机器人动作（6-DoF末端位移 + 夹爪位置） | 具身操作世界建模 | 每潜帧拼接4个动作向量（28维） |
| RealEstate10K (RE10K) | 相机轨迹 | 相机控制场景预测 | NeRF约定下逐像素光线原点/方向（720通道） |

- 视频统一缩放至 240×320，经 Wan2.2 VAE 编码至潜空间。

### 对比方法
- 主要对比：**双向短视频基线**（固定29帧滑窗预测，相同控制序列）。
- 主要结果以相对分数呈现，双向基线归一化为 1；错误指标（LPIPS、深度误差）取倒数比值使"越大越好"。

### 评估指标
- 传统指标：PSNR、SSIM、LPIPS。
- WorldArena 风格指标：外观（Appearance）、动力学（Dynamics）、几何（Geometry）、VLM功能质量、保真度（Fidelity），涵盖轨迹精度、深度精度、主体一致性、背景一致性、透视度、光度平滑度等。

## 4. 资源与算力

- **硬件**：单台配备 **8 块 GPU** 的服务器（论文未明确GPU型号，如A100/H100未说明）。
- **训练时长**：完整模型可在**数天内**训练完成。
- **精度**：bf16 混合精度训练。
- **优化器**：Muon 优化器。
- **预训练阶段**：全局 batch size 64，学习率 1×10⁻⁴。
- **长视频阶段**：全局 batch size 8，学习率从 0 线性预热至 2×10⁻⁵。
- **注意**：论文未给出具体GPU型号和精确训练小时数，仅以"several days on a single 8-GPU server"描述。

## 5. 实验数量与充分性

### 实验组数量
1. **主实验结果**：DROID 和 RE10K 两个数据集 vs 双向基线。
2. **消融实验（DROID上5组）**：
   - CFG 开/关对比；
   - 生成长度 253 vs 381 帧；
   - 去噪窗口 32块（全窗口）vs 8块（KV缓存）；
   - KV缓存大小 24/12/6 块；
   - Sink帧数 0/1/2。
3. **规模扩展实验**：RE10K 上 0.5B vs 1B vs 3B。
4. **吞吐分析**：1B 模型上 DROID 基准，全窗口 vs KV缓存，包括首块延迟和稳态 FPS。

### 充分性评估
- **优点**：消融覆盖了训练的关键设计维度（去噪窗口、KV缓存、sink、CFG）和推理关键参数，且评估指标全面（从像素保真到VLM功能质量），实验设计比较系统。
- **潜在不足**：
  - 对比方法仅有一个双向短视频基线，未与 MAGI-1、SkyReels-V2、Rolling Forcing 等代表性流式世界模型直接对比，公平性仅在同一设定内部成立。
  - 数据规模相对较小（50段held-out视频，253帧rollout），覆盖的场景多样性有限。
  - 消融实验均在 DROID 上进行，RE10K 上没有消融，跨域泛化可能掩盖部分结论的适用性。

## 6. 论文的主要结论与发现

1. **从零训练流式世界模型是可行的**：MiniWorld 在两种不同控制模态（机器人动作、相机位姿）的数据集上均优于双向短视频基线，证明"训练-推理对齐"的因果建模范式比后适配双向模型更具结构优势。
2. **显著提升体现在几何和动力学**：DROID 上轨迹精度提升249%、深度精度提升238%、LPIPS提升216%；RE10K 上光度平滑度提升89%、深度精度提升55%。说明块因果流式训练更好地保持了3D一致性和动作-状态对应关系。
3. **KV缓存流式设计是质量提升的主导因素**：8块在线窗口 + 滚动KV缓存可与32块全窗口媲美质量，同时吞吐量提升2.20倍（3.31→7.29 FPS），首块延迟降低15.2倍（74.0→4.86秒）。
4. **模型规模持续增益**：从0.5B扩展到3B参数，动态度、深度精度、图像质量和光流分数单调提升，其中动力学和几何增益最大——缩放主要强化运动建模和3D一致性。
5. **长时程漂移仍未完全解决**：扩展到381帧时，几何、VLM质量和保真度明显下降，表明错误累积仍是核心挑战。

## 7. 优点：方法或实验设计上的亮点

- **轻量透明的全流程开源基线**：发布完整数据预处理、训练、推理、评估代码和模型检查点，极大降低了视频世界模型研究门槛。
- **端到端"从头训练"设计哲学**：不使用预训练视频生成模型，避免后适配导致的训练-推理不匹配；架构简洁，直接优化流式预测目标。
- **创新性技术组合**：
  - 块因果注意力 + CoPP分块噪声调度：兼具Diffusion Forcing的异步去噪灵活性和AR-Diffusion的优化稳定性；
  - AdaLN-LoRA调制：以极小参数开销获得图层级调制能力；
  - 两阶段持续训练：先短后长的渐进式训练策略，兼顾收敛速度和长上下文适应；
  - 滚动KV缓存 + RoPE重移位：实现有界计算下的无限长生成，同时避免位置编码外推。
- **推理效率设计扎实**：通过系统级吞吐分析（区分DiT/VAE瓶颈）验证了性能提升来源，而非笼统报告端到端数字。
- **评估维度多元化**：引入 WorldArena 风格的指标组，覆盖外观、动力学、几何、VLM质量等层面，超越传统像素级指标。

## 8. 不足与局限

- **模型和数据规模有限**：与前沿视频基础模型相比，训练的模型规模（最大3B）和语料均较小，覆盖的世界动态类型有限。
- **长时程漂移依然存在**：在381帧生成中，几何精度、VLM质量和保真度明显下降；在复杂交互场景中误差积累问题仍待进一步解决。
- **缺乏与同类系统的直接对比**：未与 MAGI-1、SkyReels-V2、Rolling Forcing 等自回归视频模型在同一基准上比较，作为基线其相对"定位"不够精确。
- **硬件细节缺失**：未说明GPU具体型号，8-GPU服务器"数天"的描述对不同算力背景的复现者信息量有限。
- **消融覆盖有偏向**：全部消融在 DROID 上进行，未验证 RE10K 上的消融一致性；sink帧消融显示效果“中性”，未充分说明记忆机制的可靠增益来源。
- **推理质量与吞吐权的边界未充分探索**：流水线异步去噪深度（pipeline depth）的自由度虽声称灵活可调，但论文未给出该维度上的系统实验。
- **视觉生成质量定位**：论文承认 MiniWorld 在纯视觉质量上不及大型双向生成模型，更适合作为研究平台而非性能上限。

（完）
