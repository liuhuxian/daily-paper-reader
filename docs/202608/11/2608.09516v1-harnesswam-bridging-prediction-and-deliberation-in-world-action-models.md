---
title: "HarnessWAM: Bridging Prediction and Deliberation in World Action Models"
title_zh: HarnessWAM：弥合世界行动模型中的预测与深思
authors: "Zhaopeng Gu, Bingke Zhu, Tianxi Lin, Guibo Zhu, Yingying Chen, Kai Wang, Tingyu Yuan, Chaoyang Zhao, Zhaowen Li, Peng Su, Jinqiao Wang"
date: 2026-08-10
pdf: "https://arxiv.org/pdf/2608.09516v1"
tags: ["query:wmi"]
score: 9.0
evidence: 世界动作模型联合学习动力学与动作，并加入全局规划、状态维护与验证
tldr: World Action Models（简称WAM）在复杂具身任务中因有限视野预测而难以进行全局规划与失败恢复，存在预测与深思的鸿沟。为此，HarnessWAM提出基于视觉语言模型的任务管理器，维护基于证据的场景信念与任务图，并通过能力约束的可执行空间投影将语义计划转化为原子技能。执行时采用事件驱动的双时间尺度反馈循环，使子任务失败后能恢复状态并保留场景知识。在基准测试中，该方法取得全任务成功率百分之五十九点六、子任务成功率百分之六十九点九等最先进结果，证明了其可规划、可验证、可恢复的优越性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.09516v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1424, \"height\": 780, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09516v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1411, \"height\": 739, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.09516v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1381, \"height\": 437, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.09516v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1457, \"height\": 561, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.09516v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 641, \"height\": 292, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.09516v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1455, \"height\": 376, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.09516v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1199, \"height\": 201, \"label\": \"Table\"}]"
motivation: WAM的有限视野预测与动作生成难以满足复杂具身任务对全局规划、状态维护和失败恢复的需求，存在预测-深思鸿沟。
method: HarnessWAM采用VLM任务管理器维护证据化场景信念与结构化任务图，通过能力条件可执行空间投影生成原子技能序列，并配合事件驱动双时间尺度反馈循环执行。
result: "在RoboMemArena上全任务与子任务成功率分别达到59.6%和69.9%，在RoboCerebra Ideal上成功率为23.7%，均取得最先进结果。"
conclusion: 表明模型外部的结构化状态维护与闭环智能体决策能有效扩展WAM至可规划、可验证、可恢复的具身任务执行。
---

## 摘要
世界行动模型（WAMs）联合学习环境动态和机器人动作，将物理演化的先验引入具身控制。然而，有限时域的预测和动作生成不足以应对复杂的具身任务，这些任务需要全局规划、跨阶段状态维护、执行验证和失败恢复。我们将这种不匹配称为WAMs的预测-深思差距。为解决这一差距，我们提出HarnessWAM，一个面向WAMs的智能体框架。HarnessWAM采用基于视觉语言模型的任务管理器来维护基于证据的场景信念和结构化的任务图。能力条件下的可执行空间投影进一步将开放式的语义计划约束为满足任务依赖、具身状态约束以及底层WAM能力边界的原子技能序列。在执行过程中，HarnessWAM通过事件驱动的双时间尺度反馈回路运行：一个轻量级的进度估计器持续提供高频执行证据，而任务管理器在显著里程碑处进行深思，联合考虑当前观察、任务状态和交互历史，以决定是推进任务、获取额外观察、修订计划还是启动局部恢复。该机制使机器人能够在子任务失败后恢复其状态，并在不丢弃先前获取的场景知识的情况下继续执行。HarnessWAM在RoboMemArena上实现了59.6%的全任务成功率和69.9%的子任务成功率，在RoboCerebra Ideal上实现了23.7%的成功率，均达到最先进水平。这些结果表明，模型外部的结构化状态维护和闭环智能体决策能够有效将WAMs的局部控制能力扩展为可规划、可验证和可恢复的具身任务执行。

## Abstract
World Action Models (WAMs) jointly learn environmental dynamics and robot actions, introducing priors over physical evolution into embodied control. However, finite-horizon prediction and action generation are insufficient for complex embodied tasks that require global planning, cross-stage state maintenance, execution verification, and failure recovery. We refer to this mismatch as the prediction-deliberation gap of WAMs. To address this gap, we propose HarnessWAM, an agentic framework for WAMs. HarnessWAM employs a vision-language-model-based Task Manager to maintain an evidence-grounded scene belief and a structured task graph. A capability-conditioned executable-space projection further constrains open-ended semantic plans into sequences of atomic skills that satisfy task dependencies, embodiment-state constraints, and the capability boundary of the underlying WAM. During execution, HarnessWAM operates through an event-driven, dual-timescale feedback loop: a lightweight progress estimator continuously provides high-frequency execution evidence, while the Task Manager deliberates at salient milestones by jointly considering the current observation, task state, and interaction history to determine whether to advance the task, acquire additional observations, revise the plan, or initiate local recovery. This mechanism enables the robot to recover its state after a subtask failure and resume execution without discarding previously acquired scene knowledge. HarnessWAM achieves state-of-the-art full-task and subtask success rates of 59.6% and 69.9% on RoboMemArena, and an SR of 23.7% on RoboCerebra Ideal. These results demonstrate that model-external structured state maintenance and closed-loop agentic decision making can effectively extend the local control capabilities of WAMs into embodied task execution that is plannable, verifiable, and recoverable.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **背景与动机**：世界行动模型（WAM）能够联合建模未来观测、环境状态与机器人动作，为具身控制提供物理演化先验和局部操作能力，但其本质是有限时域的预测与动作生成。
- **核心问题**：复杂具身任务需要全局规划、跨阶段状态维护、执行验证和失败恢复，而WAM本身不维护任务级全局状态，导致“预测—深思差距”（prediction–deliberation gap）。
- **整体含义**：论文主张，仅改进WAM内部动力学表示不够，还需要一个位于模型外部的结构化智能体运行时；通过外部状态估计、闭环决策与恢复机制，将WAM的局部控制能力扩展为可规划、可验证、可恢复的具身任务执行体系。

## 2. 方法论

- **核心思想**：构建一个模型无关的Agentic框架HarnessWAM，将WAM作为可调用的局部技能执行器，由外部任务管理器负责全局推理、状态维护、计划修订与失败恢复。
- **关键技术细节**：
  - **证据化场景信念（Evidence-grounded Scene Belief）**：每个场景事实记为 `(subject, predicate, object, value, epistemic_status, confidence, evidence)`，区分“未观测”与“观测为假”，即使物体被遮挡，也不丢失先前取得的证据。
  - **任务记忆与结构化任务图**：维护 `M=(M_task, M_evidence)`；任务图 `G=(V,E,X,β)` 包含执行依赖、未解析符号变量及变量到实体的绑定；节点分为运动节点和认知节点，认知节点负责信息收集、验证与绑定。
  - **能力条件可执行空间投影**：定义参数化操作本体 `P*`（运动、抓取、接触、关节物体、装配、工具）；对特定WAM筛选出已验证技能集 `P_W`；通过确定性计划编译器，将VLM生成的语义图投影到 `Π_{L(P_W)∩F(z_k)}(G_vlm)`，强制检查参数类型、依赖、前置条件、单臂持有状态、图无环等约束。
  - **历史不变性约束**：计划修订只允许修改未执行后缀，满足 `G_{k+1}|V_executed_k = G_k|V_executed_k`，保持物理历史一致性。
  - **事件驱动双时间尺度反馈回路**：
    - 快时间尺度：WAM连续生成有限时域动作块，轻量进度估计器 `F_φ` 从最近L帧双视角RGB与技能指令预测连续进度、完成概率和进度区间分布。
    - 慢时间尺度：任务管理器仅在里程碑事件、超时、状态变化等时机被触发，联合当前观测、场景信念、任务图、记忆与进度趋势，输出决策 `d ∈ {continue, advance, observe, replan, recover, terminate}`。
  - **局部具身状态恢复**：每个运动节点开始时记录手臂关节状态与夹爪状态，执行失败或预算耗尽时通过多步控制恢复到节点起始位形，但保留环境、场景信念和已获取信息。
- **算法流程**（文字说明）：初始化场景信念和任务记忆 → VLM生成初始语义计划 → 投影为可执行技能图 → 循环选择就绪节点；若是认知节点则采集证据并更新信念/绑定/图后缀，若是运动节点则激活WAM执行；高速进度估计驱动事件触发，事件时由VLM任务管理器做完整推理，决定继续、推进、观察、重规划、恢复或终止；全部必需节点完成后做最终视觉验证并判定成功。

## 3. 实验设计

- **基准/数据集**：
  - **RoboMemArena**：26个长时程操作任务，平均轨迹长度1076个环境步，68.9%的子任务依赖历史信息；包含多物体转移、遮挡、计数、顺序执行四类任务。
  - **RoboCerebra Ideal**：长时程组合操作与高层推理的子集。
- **对比方法**：
  - RoboMemArena：π0.5、HiF-VLA、MemoryVLA、MemER、PrediMem；另设两个同WAM诊断基线：WAM + Whole Task（直接给全局指令）、WAM + Static Plan（仅初始化时生成一次固定线型计划）。
  - RoboCerebra Ideal：π0.5、OpenVLA、GPT-4o Planner + OpenVLA、HPE Framework。
- **评估指标**：RoboMemArena使用全任务成功率 `SR_task` 和子任务成功率 `SR_sub`；RoboCerebra按官方协议使用 `SR_RC`（关键物体状态转换平均完成率）。
- **实现细节**：底层WAM采用LingBot-VA（架构不变，分别在各基准训练集微调）；任务管理器为Qwen3-VL-32B-Instruct（无任务微调）；进度估计器用SigLIP2-base编码器+四层因果Transformer；每个任务20条rollout，种子/初始状态/观测接口/预算一致。

## 4. 资源与算力

- 论文**未明确报告具体算力信息**，包括GPU型号、数量、训练时长、参数量规模、能耗等。
- 仅能从描述推断：底层WAM为LingBot-VA，任务管理器使用32B参数的Qwen3-VL-32B-Instruct，进度估计器基于SigLIP2-base；这些模型均需要较高推理成本，但文中未给出硬件配置或时间成本。

## 5. 实验数量与充分性

- **实验规模**：
  - RoboMemArena上完成主实验（26任务×20 rollout）和5组消融（w/o task state、w/o executable projection、w/o progress events、progress-only switching、w/o recovery）；
  - RoboCerebra Ideal上完成主实验（对比4个基线）；
  - 另有可执行空间投影的离线计划级诊断（语法、依赖、绑定、可执行性4个指标）。
  - 这些实验构成了主结果、消融结果、计划级诊断和定性案例四层证据。
- **充分性评估**：
  - 较充分：主实验覆盖两个互补基准，且在同一WAM检查点下进行受控对比，隔离了任务编排的贡献；消融覆盖了任务状态、投影、事件触发、验证方式和恢复五个关键组件。
  - 不足之处：仅评测了两个仿真基准，没有真实机器人实验；RoboCerebra上的增益相对较小（+1.78与+2.60个百分点）；消融集中在RoboMemArena，未在RoboCerebra上系统消融；未报告多次运行方差或显著性检验。

## 6. 主要结论与发现

- 在RoboMemArena上，HarnessWAM取得全任务成功率59.6%、子任务成功率69.9%，比PrediMem分别高21.1和14.7个百分点；在RoboCerebra Ideal上取得23.7%，优于所有基线。
- 初始分解本身也有帮助（WAM + Static Plan比WAM + Whole Task高3.5点全任务成功率），但HarnessWAM比WAM + Static Plan再提升11.7点，说明持久任务状态和闭环管理不可或缺。
- 可执行空间投影贡献最大：去掉后全任务成功率从59.6%暴跌至18.5%；计划级诊断显示，原始VLM计划可执行性仅13.8%，词汇归一化提升到42.3%，完整投影后达到72.9%。
- 事件驱动触发机制显著优于固定频率触发；进度估计不能替代语义验证，否则顺序执行子任务成功率高但全任务成功率大幅下降。
- 局部恢复能在子任务失败后保留场景知识并提升长序列任务成功率。
- 总体支持：模型外部的结构化状态维护与闭环智能体决策能有效弥补WAM在全局规划、验证和恢复上的不足。

## 7. 优点

- 问题界定清晰：明确提出“预测—深思差距”，将WAM能力扩展定位为模型外部编排问题，而非修改WAM内部架构。
- 框架设计有层次：证据化信念、符号变量延迟绑定、能力条件投影、确定性计划编译器，都有效地把开放式VLM规划约束到可执行的WAM技能空间。
- 双时间尺度机制兼具效率与准确性：高频进度估计驱动事件触发，低频VLM只做稀疏且关键的语义验证，避免逐帧调用大模型。
- 工程上可直接落地：不改变WAM架构，底层WAM可替换，具备很强的兼容性。
- 实验设计严谨：受控对比（相同WAM检查点）+多角度消融+计划级诊断，能够把各因素贡献分离清楚。

## 8. 不足与局限

- **场景局限**：仅在RoboMemArena和RoboCerebra两个仿真基准上验证，未涉及真实物理世界或更广泛的任务族，泛化性存疑。
- **底层模型单一**：所有实验共用LingBot-VA一个WAM，未验证框架在多种WAM（如DreamZero、Fast-WAM等）上的稳定性。
- **RoboCerebra增益有限**：相比GPT-4o Planner + OpenVLA仅高1.78点，可能说明在静态全可观测任务上，框架的优势主要来自执行可靠性而非规划能力。
- **算力与开销未报告**：VLM任务管理器（32B）和进度估计器的计算成本、延迟、显存占用等关键工程指标缺失，影响实际应用判断。
- **依赖VLM能力**：场景信念更新、失败识别和恢复决策都依赖VLM的视觉语言推理质量，VLM错误可能传播为任务级失败。
- **恢复策略较朴素**：仅恢复机械臂/夹爪到节点初始位形，未处理物体被移动等环境状态变化；面对复杂物理后果可能不足。
- **部分指标退步**：计数任务子任务成功率比Static Plan低1.9个百分点，说明中间状态验证未完全解决计数类局部偏差问题。
- **未提供不确定性量化**：决策过程没有显式不确定度校准，可能在高噪声观测下产生错误的事件触发或误判。

（完）
