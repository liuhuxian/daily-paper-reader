---
title: "τ: Learning Touch-Augmented Vision-Language-Action Models from Future Visual Supervision"
title_zh: τ：从未来视觉监督中学习触觉增强的视觉-语言-动作模型
authors: "Ning Cheng, Jinan Xu, Wanlin Li, Yangzhi Chen, Jing Gao, Yiqun Wang, Kelan Peng, Wenjuan Han"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24485v2"
tags: ["query:wmi"]
score: 8.0
evidence: 通过未来视觉监督学习触觉表示，增强面向接触丰富操作的视觉-语言-动作模型。
tldr: 触觉对接触丰富的操作至关重要，但现有VLA模型难以在有限数据下学习有效的触觉表示。τ框架受JEPA启发，从未来视觉监督中学习动作条件的时空触觉表示，并在训练时与视觉语言特征融合用于动作生成，推断时无额外开销。在四种接触丰富任务上，τ优于现有模型并泛化到新物体和场景，提升了操作性能与鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24485v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1810, \"height\": 1004, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24485v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1839, \"height\": 355, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24485v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1820, \"height\": 1219, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24485v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 877, \"height\": 581, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24485v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 832, \"height\": 476, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24485v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1822, \"height\": 438, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24485v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1810, \"height\": 296, \"label\": \"Table\"}]"
motivation: 现有方法仅利用瞬时接触或低维力/力矩序列，未充分挖掘高维触觉信号，且难以适配预训练VLA模型。
method: 提出τ，利用未来视觉监督（JEPA式）在潜空间学习动作条件时空触觉表示，并与视觉语言特征融合生成动作。
result: 在自建TacAura数据集上，τ在四类接触丰富任务中优于现有模型，并泛化到未见物体与场景。
conclusion: τ验证了未来视觉监督用于触觉表示学习的有效性，为触觉增强VLA提供了新范式。
---

## 摘要
将触觉感知融入视觉-语言-动作（VLA）模型有望实现接触丰富的操作，在这些操作中，仅凭视觉观察往往无法捕捉到关于物理交互的关键线索。然而，在任务特定数据有限的情况下，学习信息丰富的触觉表示并将其有效适配到预训练的VLA模型仍然具有挑战性。现有方法要么专注于瞬时接触状态，要么使用6维力/力矩序列对时间交互动态进行建模，而高维触觉信号仍未得到充分探索。为了解决这些挑战，我们提出了τ，一个触觉增强的VLA框架，它受联合嵌入预测架构（JEPA）启发，从未来视觉监督中学习动作条件的时空触觉表示，并将其与视觉-语言特征融合以生成动作。这种监督在潜在空间中运行，仅在训练期间使用，不增加部署开销。我们还引入了TacAura，一个在四个代表性接触丰富操作任务中同步视觉、本体感觉和基于视觉的触觉信号的数据集。实验表明，τ优于现有模型，并能泛化到未见过的物体和场景，提供改进的操作性能和鲁棒性。

## Abstract
Incorporating tactile sensing into Vision-Language-Action (VLA) models holds promise for contact-rich manipulation, where visual observations alone often fail to capture critical cues about physical interactions. However, learning informative tactile representation while effectively adapting it to pretrained VLA models remains challenging under limited task-specific data. Existing methods either focus on instantaneous contact states or model temporal interaction dynamics using 6D wrench sequences, leaving high-dimensional tactile signals underexplored. To address these challenges, we present τ, a touch-augmented VLA framework that learns an action-conditioned spatiotemporal tactile representation from future visual supervision inspired by the Joint-Embedding Predictive Architecture (JEPA), and fuses it with vision-language features for action generation. This supervision operates in latent space and is used only during training, adding no deployment overhead. We also introduce TacAura, a dataset of synchronized vision, proprioception, and vision-based tactile signals across four representative contact-rich manipulation tasks. Experiments show that τ outperforms existing models and generalizes to unseen objects and scenes, delivering improved manipulation performance and robustness.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：视觉-语言-动作（VLA）模型通过大规模多模态预训练，已成为通用机器人操作的新范式。但其主要依赖视觉信息，在接触丰富（contact-rich）操作中，视觉无法充分刻画接触状态、力分布、形变等关键物理交互线索。
- **问题**：将触觉传感融入VLA模型虽有前景，但面临两大挑战：(1) 在任务特定数据有限时学习信息丰富的触觉表示；(2) 如何将这些表示有效适配到预训练VLA模型中。
- **现有方法不足**：已有工作或只关注瞬时接触状态，或用6维力/力矩序列建模时间动态，而高维视觉触觉信号（如法向/切向变形图）的时空交互动态尚未被充分挖掘。
- **论文目标**：提出τ框架，从“未来视觉监督”中学习动作条件（action-conditioned）的时空触觉表示，在潜空间进行预测式自监督学习，从而在不增加部署开销的前提下增强VLA模型的接触感知能力。

## 2. 论文提出的方法论

- **总体架构**：τ基于预训练VLA模型π0.5，由三部分组成：
  - 预训练π0.5 VLA骨干，负责多模态融合与动作生成；
  - 触觉编码与适配模块，将原始触觉信号映射到VLA的潜空间；
  - 辅助的JEPA式预测自监督分支，仅训练时使用，推理时移除。

- **触觉编码与适配**：
  - 输入为左右手指上安装的视觉触觉传感器（DM-Tac WS）提供的单通道法向图和双通道切向图。
  - 触觉编码器 \(E_{tou}\) 以π0.5视觉编码器权重初始化，左右共享，输出触觉特征 \(z^{touch}_t\)。
  - 通过可学习线性适配器 \(A_{tou}\) 将触觉特征投影到与VLA语义空间兼容的令牌序列 \(Z^{touch}_t\)。
  - 触觉令牌与视觉、语言令牌拼接，送入LLM和动作专家，预测未来动作块。

- **JEPA式预测自监督分支**：
  - 核心思想：不预测像素，而在潜空间预测未来视觉特征的变化。
  - 输入为当前触觉令牌 \(Z^{touch}_t\) 与动作序列令牌 \(Z^{action}_{t:t+H}\)（由动作编码器 \(E_{act}\) 编码），经轻量MLP预测器 \(P\) 预测未来视觉特征 \(\hat{z}^{vision}_{t+\Delta_k}\)。
  - 预测目标为相对于当前视觉特征的变化量 \(\Delta z^{vision}_{t+\Delta_k}\)（使用停止梯度），并采用加权余弦相似度损失。
  - 权重 \(w_{t+\Delta_k}\) 由左右触觉信号的变化幅度决定，以强调接触转换明显的时刻。

- **训练策略**：
  - 联合优化模仿学习损失 \(L_{IL}\)（基于条件流匹配的动作去噪）与自监督损失 \(L_{SSL}\)，总损失 \(L_{total}=L_{IL}+\lambda L_{SSL}\)，\(\lambda=0.3\)。
  - 自监督分支仅用于训练，推理时无额外参数和计算开销。

## 3. 实验设计

- **数据集**：自建TacAura数据集，包含四个代表性接触丰富操作任务：
  - 插头插入（Plug Insertion）、USB插入（USB Insertion）、图章按压（Stamp Press）、白板擦除（Whiteboard Erasing）。
  - 每个任务收集100条人类遥操作演示，包含多视角RGB-D、本体感觉和左右手触觉信号，并同步对齐、降采样至10Hz。

- **Benchmark与评估**：
  - 每个任务按执行阶段拆分成功率（如抓取/对齐/插入、拾取/接触/按压或擦除）。
  - 对比模型：视觉VLA基线π0、π0.5，以及触觉感知基线ForceVLA†、ForceFlow†、T-Rex†（†表示针对该触觉设置做了适配）。
  - τ有3个变体：τ-Wrist Sup.（腕部视图监督）、τ-Front Sup.（前视监督）、τ-DualView Sup.（双视图监督）。

- **泛化测试**：
  - 对USB插入和白板擦除，进行零样本物体泛化（两个未见物体）和场景泛化（两个未见干扰物集合）。

- **消融实验**：
  - 在最佳变体τ-Wrist Sup.上，分别去除动作序列条件、去除预测自监督、去除触觉模块。

## 4. 资源与算力

- 论文明确说明训练步数为30,000步，但**未提及**GPU型号、数量、训练时长、显存等硬件算力信息，也未说明训练时间成本。因此无法对资源消耗进行具体总结。

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验：4任务 × 约6种模型/变体 × 20次试验，每个任务分三阶段统计成功率（表1）。
  - 泛化实验：2个任务 × 2未见物体 + 2未见干扰物集合（图5）。
  - 消融实验：3种剥离设置，共4组模型对比（表2）。
  - 还包含定性实验展示（图3）。

- **充分性评价**：
  - 优点：覆盖多种接触特征的任务，分阶段评估能定位失败环节；对比了普通VLA与触觉感知方法；设置了消融和泛化实验，整体较系统。
  - 不足：每个任务仅20次试验，未给出方差或置信区间；数据规模较小（每任务100演示）；未与触觉世界模型（如Dream-Tac等）比较；基线方法未使用触觉，与τ的对比存在一定不对称性。

## 6. 论文的主要结论与发现

- τ的最佳变体（τ-Wrist Sup.）平均全任务成功率达71.25%，比最强基线T-Rex†（31.25%）高出40个百分点；其余变体也大幅超越基线。
- 基线模型在任务最终执行阶段（如插入、完整按压/擦除）成功率骤降，而τ能更好地保持从中间阶段到最终完成的高成功率。
- 监督视图具有任务依赖性：前视监督更适合插孔定位（插头/USB插入），腕视监督更适合力相关任务（图章按压、白板擦除）；双视图监督反而效果较差，说明简单融合策略不足。
- 泛化实验表明：白板擦除对未见物体和场景几乎无退化（成功率保持90-95%），而USB插入在未见物体和干扰物下分别平均降至30%和22.5%，说明该任务对几何和视觉干扰更敏感。
- 消融显示：触觉模块贡献最大（移除后平均成功率下降42.5个百分点），预测自监督次之（下降20个百分点），动作序列条件第三（下降12.5个百分点）。

## 7. 优点

- **方法创新**：首次将JEPA式未来视觉预测用于触觉VLA表示学习，在潜空间提供监督，避免像素级重建，提升训练效率与表示质量。
- **动作条件预测**：触觉表示以后续动作为条件，使编码的交互动态与控制目标直接相关。
- **零推理开销**：自监督分支仅训练时使用，不增加部署计算负担。
- **数据基础设施**：构建了完整的遥操作采集、多流同步转换工具和TacAura数据集，并计划开源，利于复现和后续研究。
- **实验设计细致**：分阶段成功率揭示性能差异所在，且包含多视图变体、泛化与消融分析，结论较有说服力。

## 8. 不足与局限

- **对比不充分**：未与触觉世界模型方法比较，作者归因于算力限制；现有基线均以VLA为基础，未覆盖更多前沿触觉策略。
- **泛化范围有限**：仅评估了同任务下的物体和场景泛化，未探索跨任务、跨实体、跨触觉传感器的迁移能力。
- **数据规模限制**：每任务仅100条演示，可能不足以充分发挥预训练大模型的潜力，也限制了在更多任务上的验证。
- **多视图融合不足**：τ-DualView Sup.性能低于单视图变体，说明双视图监督融合策略过于简单，存在改进空间。
- **统计稳健性有限**：每个设置仅20次试验，未报告方差或置信区间，结果可能受随机因素影响。
- **公平性风险**：基线方法（如π0、π0.5）未接受触觉输入，而τ专门针对触觉设计，尽管对部分基线做了适配，但对比条件本质上不完全对等。

（完）
