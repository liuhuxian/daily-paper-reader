---
title: "MODIP: Efficient Model-Based Optimization for Diffusion Policies"
title_zh: MODIP：用于扩散策略的高效基于模型的优化
authors: "Zakariae El Asri, Philippe Gratias-Quiquandon, Nicolas Thome, Olivier Sigaud"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10825v1"
tags: ["query:wmi"]
score: 9.0
evidence: 结合行为克隆与世界模型的基于模型的扩散策略优化
tldr: 扩散策略（DP）在行为克隆（BC）中表现优异，但直接强化学习（RL）微调因多步去噪过程而困难。本文提出MODIP框架，利用世界模型（WM）进行模型预测控制（MPC）生成高质量轨迹，作为监督目标微调DP，并采用终端状态值和策略无关的TD目标提升训练与推理效率。在D4RL和RoboMimic任务上，MODIP超越了BC性能，与DP的RL微调方法及强模型基线TD-MPC2相比具有竞争力或更优。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10825v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1434, \"height\": 724, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10825v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1430, \"height\": 929, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10825v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1366, \"height\": 1276, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10825v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 362, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10825v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 266, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10825v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1083, \"height\": 137, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10825v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1461, \"height\": 174, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10825v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1427, \"height\": 1620, \"label\": \"Table\"}]"
motivation: 扩散策略在行为克隆中成功，但直接强化学习微调面临多步去噪的挑战。
method: MODIP使用世界模型进行模型预测控制生成轨迹，作为监督目标微调扩散策略，并引入终端状态值和策略无关的TD目标以提高效率。
result: 在D4RL和RoboMimic任务上，MODIP超越行为克隆，与扩散策略RL微调及TD-MPC2等基线相当或更优。
conclusion: MODIP为扩散策略提供了一种高效、稳定的离线到在线微调方案，兼顾简单性和性能。
---

## 摘要
扩散策略（DPs）已成为机器人学习中富有表现力的策略表示，通常与行为克隆（BC）等模仿学习方法一起使用。然而，虽然它们的成功主要局限于BC，但直接进行强化学习（RL）微调仍然具有挑战性，因为动作是通过多步去噪过程生成的。在这项工作中，我们提出了MODIP，一个用于扩散策略的离线到在线微调的框架。MODIP并非直接将RL应用于DPs，而是利用世界模型（WM）来指导策略适应，并保持BC的简单性和稳定性。我们利用模型预测控制（MPC）在WM内生成高质量轨迹，并将其作为监督目标来微调DP。为了使MPC规划高效，MODIP使用终端状态值代替策略依赖的状态-动作值，减少了推理时间。此外，MODIP使用策略无关的TD目标训练评论家，减少了训练时间。在D4RL（MuJoCo、Kitchen）和RoboMimic任务上的实验表明，MODIP改进了超越BC的扩散策略，并且与扩散策略RL微调方法以及如TD-MPC2等强模型基基线相比具有竞争力或更优性能。

## Abstract
Diffusion policies (DPs) have emerged as expressive policy representations for robot learning, often used with imitation learning methods such as behavioral cloning (BC). However, while their success has largely been confined to BC, direct reinforcement learning (RL) fine-tuning remains challenging because actions are generated through a multi-step denoising process. In this work, we propose MODIP, a framework for the offline-to-online fine-tuning of DPs. Rather than directly applying RL to the DPs, MODIP leverages a world model (WM) to guide policy adaptation and keeps the simplicity and stability of BC. We utilize model predictive control (MPC) to generate high-quality trajectories within the WM, and use them as supervised targets for fine-tuning the DP. To make MPC planning efficient, MODIP uses a terminal state value instead of a policy-dependent state-action value, reducing inference time. Additionally, MODIP trains critics with policy-independent TD targets, reducing training time. Experiments on D4RL (MuJoCo, Kitchen) and RoboMimic tasks show that MODIP improves diffusion policies beyond BC, and is competitive with or outperforms diffusion policy RL fine-tuning methods and strong model-based baselines such as TD-MPC2.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题**：扩散策略（Diffusion Policies, DPs）在模仿学习（如行为克隆 BC）中表现优秀，能够建模多模态动作分布。然而，当需要利用奖励信号进行强化学习（RL）微调时，直接应用 RL 面临挑战：DP 通过多步去噪生成动作，导致策略优化复杂、不稳定且计算成本高。
- **动机**：希望在不牺牲 BC 稳定性的前提下，利用在线交互进一步提升 DP 性能。作者提出一种间接的、基于模型的优化方式：通过世界模型（World Model）进行模型预测控制（MPC）生成高质量轨迹，再用这些轨迹监督微调 DP，从而避免直接 RL 的复杂性与不稳定性。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：MODIP = 离线预训练（DP + 世界模型） + 在线 MPC 引导的轨迹优化 + 蒸馏（Distillation）回 DP。使用终端状态值 V(s) 而非 Q(s,π(s)) 降低推理开销；使用策略无关（policy-independent）的 TD 目标训练评论家，降低训练开销。
- **关键技术细节**：
  1. **世界模型组件**：编码器 h_η（观测→隐状态 z）、潜在动力学模型 f_θ（预测下一隐状态）、奖励模型 R̂_ξ。模型通过联合损失训练：L_model = λ_cons L_cons + λ_rew L_rew + λ_value (L_Q + L_{V,A})。
  2. **DP 作为 MPC 先验**：在 MPC 中，一部分候选动作序列由 DP 生成（作为有信息的先验），其余从高斯分布采样。通过 MPPI 优化轨迹。目标函数包含 H_mpc 步内的预测奖励和终端值：max_A [∑_{k=0}^{H_mpc-1} γ^k R̂_ξ(z_t+k, a_{t+k}) + γ^{H_mpc} V_ψ(z_{t+H_mpc})]。
  3. **终端状态值 V(z) 代替 Q(z,π(z))**：避免在终端状态调用 DP 的多步去噪，单次前向即可得到值估计，减少推理时间（实验中约 2.90 倍加速）。
  4. **策略无关的评论家学习**：TD 目标 y_t = r_t + γ(1-d_t) min_j V_ψ̄_j(h_η̄(s_{t+1}))，不依赖于当前策略的动作，从而避免训练中频繁调用 DP。评论家包括 Q 网络、V 网络和优势网络，使用 AFU 风格的值-优势分解。
  5. **蒸馏流程**：在线交互时，MPC 规划器生成轨迹存入在线回放缓冲；训练时混合离线数据与在线数据（时间变化混合比例 β(t)）；DP 通过标准去噪损失（BC 损失）在混合数据上更新，并加入冻结的 BC 策略的正则化项（λ(t) 逐渐衰减）。
- **算法流程**：离线预训练 DP 和世界模型 → 在线交互：状态 s_t → 编码 z_t → DP 提供动作先验 + MPPI 在潜在空间优化 → 执行第一个动作 → 存储轨迹 → 定期从混合缓存采样更新 DP 和世界模型/评论家。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法
- **数据集/场景**：
  - D4RL MuJoCo 任务：HalfCheetah、Hopper、Walker2d（密集奖励，连续控制）。
  - D4RL Kitchen 任务：Complete 和 Partial（长视距操作，稀疏奖励）。
  - RoboMimic 状态操作任务：Lift、Can、Square（多阶段精确操作，状态输入）。
- **Benchmark**：累计回报（MuJoCo）、任务成功率（Kitchen 和 RoboMimic）。
- **对比方法**：
  - DP 相关 RL 微调方法：BC（预训练行为克隆）、DQL（Diffusion-QL）、DPPO、PA-RL、DSRL。
  - 基于模型的基线：TD-MPC2（基于 MLP 策略的混合规划）。
  - 所有方法在固定计算预算下评估。

### 4. 资源与算力
- 文中未明确说明 GPU 型号、数量或具体算力描述。仅报告了训练时间：在 HalfCheetah 上，策略无关变体总训练时间约 25 小时，策略耦合变体约 40 小时。未提及具体硬件。

### 5. 实验数量与充分性
- **实验数量**：主实验覆盖 3 个基准（7 个具体任务），与 6 种基线对比（含消融实验前一共约 7 种方法）。消融实验包含：
  - 混合规划 vs. 仅策略（不进行 MPC 优化）。
  - 终端值 V(s) vs. Q(z,π(z))（性能和推理时间对比）。
  - 策略无关 vs. 策略耦合评论家（训练时间对比）。
  - 离线预训练 vs. 无离线预训练。
  - 政策先验：DP vs. 高斯 MLP。
- **充分性**：实验设计比较全面，覆盖了不同任务类型（密集/稀疏奖励、长视距、操作），且消融实验验证了各组件的贡献。对比基线覆盖了 DP 的多种 RL 微调方法和强模型基线，结果表格和曲线显示统计误差（标准差）。**公平性**：所有方法在同一计算资源下比较；TD-MPC2 采用相同离线预训练流程；主要结果表（Table 1）中 RoboMimic 的 DSRL 成绩引用自原文（Wagenmaker et al., 2025），其他方法自行复现（据附录 C）。总体较客观。

### 6. 论文的主要结论与发现
- MODIP 在 MuJoCo、Kitchen 和 RoboMimic 上显著优于 BC 基线，与直接 RL 微调方法（DPPO、DSRL）相比有竞争力，且在 Kitchen 任务上最优。
- 终端状态值 V(s) 相比 Q(z,π(z)) 在性能和推理时间上均有优势（2.90 倍加速且表现更好）。
- 策略无关的评论家训练相比策略耦合节省约 1.6 倍总训练时间。
- 离线预训练和 DP 作为 MPC 先验对性能有重要贡献（尤其是在早期探索困难的任务中）。
- 相比于 TD-MPC2（MLP 策略），使用 DP 先验能提升效果，证明 DP 的分布建模能力有益于混合规划。

### 7. 优点：方法或实验设计上有哪些亮点
- **方法优点**：
  - 间接优化：保持监督学习的稳定性，避免了直接 RL 的困难（多步去噪带来的不稳定）。
  - 计算效率高：终端 V(s) 和策略无关评论家显著降低推理和训练开销。
  - 可扩展：混合使用离线/在线数据（RLPD 风格），时间变化混合比和 BC 正则化保障了从模仿到优化的平滑过渡。
  - 通用性：可结合不同规划器（MPPI）和策略表示（DP）。
- **实验优点**：
  - 消融实验覆盖全面，验证了每个设计选择（混合规划、终端值、策略耦合、离线预训练、策略类型）。
  - 对比基线包括最新 DP 微调方法和强模型基线（TD-MPC2），且在同一预算下比较。
  - 提供了学习曲线（附录 E），展示了在线改进过程。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **局限**：
  - **仅状态输入**：所有实验使用低维状态向量，未评估图像输入（像素）场景。作者明确写道：\"assessing the potential of MODIP to learn from images is an obvious next step for future work\"。
  - **计算资源未报告**：缺少 GPU 型号、数量等细节，可复现性稍弱。
  - **实验规模**：主要实验集中在 D4RL 和 RoboMimic 的 7 个任务，对于真实机器人部署、高维视觉任务的泛化性未验证。
  - **DP 先验依赖**：如果离线数据集质量很低或分布偏差大，DP 先验可能不准确，影响 MPC 初始效果；虽然 BC 正则化可缓解，但未针对低质量数据评估。
  - **超参数敏感性**：许多超参数（如混合比例、规划样本数、迭代次数）未详细分析其敏感性。
  - **公平性局限**：部分基线（如 RoboMimic 上 DSRL）结果引自原论文，可能因实现细节不同导致偏差。但总体偏差风险较低。
- **潜在偏差**：作者单位均为法国索邦大学，无明显的利益冲突。但方法名称 MODIP 与先前工作类似（？），未发现明显偏见。

（完）
