---
title: "MetaWorld: Scaling Multi-Agent Video World Model from Single-view Video Data"
title_zh: MetaWorld：从单视角视频数据扩展多智能体视频世界模型
authors: "Teng Hu, Mingchun Lu, Yating Wang, Jiangning Zhang, Jinkun Hao, Ye Pan, Ran Yi, Lizhuang Ma, Dacheng Tao"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.02753v1"
tags: ["query:wmi"]
score: 9.0
evidence: 从单视角视频构建多智能体视频世界模型
tldr: 多智能体视频世界模型面临多视角数据稀缺和跨视角世界状态对齐的挑战。MetaWorld通过单目世界状态展开（MWSU）从单视角视频中分解相机运动与主体轨迹，无需多相机数据；结合主体感知世界生成器和世界状态对齐（WSA）机制，实现多视角视频生成。实验证明该方法在跨视角一致性和身份保真度上显著优于现有方法，为开放域多智能体视频世界建模提供了可扩展的物理驱动范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.02753v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1451, \"height\": 686, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02753v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 602, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02753v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1451, \"height\": 806, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02753v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1451, \"height\": 1555, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02753v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1450, \"height\": 1553, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02753v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1457, \"height\": 1854, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02753v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1457, \"height\": 2273, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.02753v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1377, \"height\": 303, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02753v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1306, \"height\": 327, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.02753v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1031, \"height\": 691, \"label\": \"Table\"}]"
motivation: 多智能体视频世界模型因多视角数据采集成本高和跨视角世界状态对齐困难而发展受限。
method: 提出MetaWorld框架，包含单目世界状态展开、主体感知世界生成器和逐帧交叉注意力世界状态对齐机制。
result: 在跨视角静态一致性和动态运动一致性上取得最优，身份保真度显著提升。
conclusion: MetaWorld为开放域多智能体视频世界建模提供了高效、可扩展的物理驱动解决方案。
---

## 摘要
视频世界模型是具身智能和元宇宙的基础生成技术，但现有方法本质上局限于单智能体从单一视角观察。将这些模型扩展到多智能体设置会引入两个关键挑战：数据稀缺性（协调的多视角记录在通用开放域场景中收集成本极高）和世界状态对齐（独立生成的视频流无法确保共享物理环境和事件在不同视角下一致演化）。为了解决这些挑战，我们提出了MetaWorld，一个新颖的框架，直接从单视角视频将多智能体视频世界模型扩展到开放域环境。首先，我们引入单目世界状态展开（MWSU）来显式地将单目视频分解为相机操作者的自我运动和可见主体的空间轨迹。这种相机-轨迹分解自然地提取共享3D空间内同步的多智能体运动数据，完全绕过了多相机设置的需求。其次，为了实现精确的视觉控制，我们开发了主体感知世界生成器，以基于每个智能体身份图像的条件进行外观驱动模拟。最后，为确保两个视角都基于相同的物理现实，我们提出世界状态对齐（WSA），一种在每个transformer层插入的逐帧分支间交叉注意力机制。通过联合同步去噪过程，WSA强制实现静态几何一致性和动态运动一致性，确保共享的3D环境和物理事件在两个自我中心视角中保持良好对齐。大量实验表明，MetaWorld实现了优越的跨视角一致性和身份保真度，为多智能体视频世界建模建立了一个高度可扩展、物理驱动的范式。

## Abstract
Video world models are a foundational generative technology for embodied AI and the Metaverse, yet existing approaches are inherently limited to a single agent observing from a single perspective. Extending these models to multi-agent settings introduces two critical challenges: data scarcity (coordinated multi-view recordings are prohibitively expensive to collect for general open-domain scenarios) and world state alignment (independently generated video streams cannot ensure that shared physical environments and events evolve consistently across views). To address these challenges, we propose MetaWorld, a novel framework that scales multi-agent video world models to open-domain environments directly from single-view videos. First, we introduce Monocular World-State Unrolling (MWSU) to explicitly decompose monocular footage into the camera operator's ego-motion and the visible subject's spatial trajectory. This camera-trajectory decomposition naturally extracts synchronized multi-agent motion data within a shared 3D space, completely bypassing the need for multi-camera setups. Second, for precise visual control, we develop the Subject-Aware World Generator to enable appearance-driven simulation conditioned on per-agent identity images. Finally, to ensure both views are grounded in the identical physical reality, we propose World-State Alignment, a per-frame inter-branch cross-attention mechanism inserted at every transformer layer of the video DiT. By jointly synchronizing the denoising process, WSA enforces both static geometric consistency and dynamic motion consistency, encouraging that the shared 3D environment and physical events remain well-aligned across both egocentric views. Extensive experiments demonstrate that MetaWorld achieves superior cross-view consistency and identity fidelity, establishing a highly scalable, physics-driven paradigm for multi-agent video world modeling.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：视频世界模型是具身智能和元宇宙的基础技术，但现有方法仅支持单智能体从单一视角观察世界。将世界模型扩展到多智能体场景具有重大需求，例如协同机器人学习、同步虚拟环境等。
- **关键挑战**：
  - **数据稀缺性**：协调的多视角同步录制在开放域场景中成本极高，难以大规模获取。
  - **世界状态对齐**：独立生成的多个视频流无法保证共享的物理环境和事件在不同视角下保持静态几何一致性和动态运动一致性。
- **整体目标**：提出MetaWorld，直接从丰富的单视角视频数据中构建多智能体视频世界模型，无需多相机设置或游戏引擎，实现开放域环境下身份一致、物理同步的多视角自我中心视频生成。

## 2. 方法论：核心思想、关键技术细节

### 2.1 核心思想

通过**单目世界状态展开（MWSU）** 从单视角视频中同时提取相机操作者的自我运动和可见主体的空间轨迹，从而获得同步的多智能体运动数据；在此基础上训练**主体感知世界生成器（SAWG）** 进行外观驱动的模拟，并通过**世界状态对齐（WSA）** 机制强制多个生成分支共享相同的物理世界状态。

### 2.2 关键技术细节

#### 2.2.1 单目世界状态展开（MWSU）

- **数据引擎**：对单视角视频自动提取：
  - 相机运动序列 \(c_i\)（使用 MoGe-2 估计）。
  - 多主体轨迹集 \(T^{(i)}_{\sim i}\)：通过 SAM 2 跟踪主体，结合深度图（Depth Anything）和相机位姿，计算主体在相机空间中的相对轨迹，并表示为移动3D高斯球。
  - 身份参考图像集 \(I_{\sim i}\)：从跟踪结果中裁剪高质量前景图像。
- **背景场景构建**（两种策略）：
  - **锚帧背景建模**：仅用首帧建立部分环境先验，训练模型进行场景补全和外推。
  - **整体背景聚合**：融合所有帧的静态背景（过滤动态物体），提供完整3D先验，增强空间一致性。
- **推理时的多智能体生成**：给定全局轨迹和身份图像，为每个智能体构造条件元组 \((S_i, c_i, T^{(i)}_{\sim i}, I_{\sim i})\)，所有分支共享同一个训练好的模型 \(p_\theta\)，对称并行采样。

#### 2.2.2 主体感知世界生成器（SAWG）

- 基于 Phantom-Wan2.1 14B DiT 骨干。
- **几何状态渲染**：将3D背景和轨迹渲染为RGB条件视频和深度视频。
- **主体位置与几何接地**：VAE编码RGB条件和深度视频，拼接后通过零初始化的特征压缩网络压缩，残差加到噪声潜变量 \(z_t\) 上。
- **主体外观锚定**：将身份参考图像的VAE潜变量沿时间轴拼接，作为持久外观锚点，在每个去噪步骤提供视觉引导。

#### 2.2.3 世界状态对齐（WSA）

- **问题**：独立生成多视角视频会导致静态几何不一致和动态事件不同步。
- **方法**：在DiT的每个transformer层插入**逐帧分支间交叉注意力模块**。
  - 仅允许同一帧内空间token之间的交叉注意力，避免跨帧时间泄漏。
  - 输出投影零初始化，保护预训练先验。
- **训练策略**：使用成对双智能体交互的小规模真实多视角数据集，双向计算交叉注意力，损失为两个分支流匹配损失的平均。
- **推理扩展**：采用有向循环注意力拓扑（Agent 1→2→...→N→1），以 \(O(N)\) 复杂度高效实现 \(N\) 个智能体的全局一致性。

## 3. 实验设计

### 3.1 数据集与场景

- **单视角训练数据**：从公开可得的单视角视频中通过MWSU数据引擎自动生成训练元组，未指定特定数据集名称，但强调遵循“开放域”原则（室内、室外、动态场景等）。
- **多视角对齐数据集**：收集了小规模真实多视角视频，用于训练WSA模块（数量未明确）。
- **测试场景**：分为**Full-3D场景**（提供完整3D布局）和**Partial-3D场景**（仅首帧或稀疏视角重建），覆盖多种开放域环境。

### 3.2 评价指标

- **单视角视频质量**：使用VBench套件，包括Subject Consistency、Background Consistency、Motion Smoothness、Aesthetic Quality、Imaging Quality。
- **多视角同步指标**：
  - **Cross-View Consistency (CVC)**：基于点云重建，计算几何F-score和颜色相似性的平均值。
  - **Trajectory Consistency**：输入轨迹与生成视频中提取的实际运动之间的对齐程度。

### 3.3 对比方法

- **MultiWorld [30]**：多视角视频生成模型，使用其官方预训练权重。
- **VerseCrafter [39]**：图像到视频模型，支持相机和轨迹控制，独立渲染每个智能体的视角（使用本文生成的同一首帧作为输入，以实现公平比较）。

### 3.4 消融实验

共进行5组消融：w/o WSA、w/o Depth Video、w/o Anchor-Frame BG、w/o Holistic BG、w/o ZeroInit，均在表2中报告定量结果。

## 4. 资源与算力

- **GPU型号与数量**：32块 NVIDIA H20 GPU。
- **训练阶段**：
  - **Stage 1**（训练SAWG）：使用上述算力，优化器AdamW，学习率5×10⁻⁴，bf16混合精度。
  - **Stage 2**（加入WSA联合训练）：从Stage 1检查点初始化，相同配置。
- **训练时长**：论文未明确提供训练总时长。

## 5. 实验数量与充分性

- **定量比较**：主实验表1涵盖了Full-3D和Partial-3D两种设置，与两个基线对比，报告了7个指标。
- **消融实验**：表2包含5个消融变体，每个变体在所有7个指标上进行评估，共5×7=35个数据点。
- **定性展示**：展示了多组多视角生成结果（图3、图6、图7），以及单视角生成结果（图4、图5），覆盖室内、室外、动态场景等多样化环境。
- **公平性**：与VerseCrafter比较时，使用了相同的第一帧进行初始化，避免不公平优势；MultiWorld使用官方权重。消融实验控制变量完整。
- **充分性判断**：实验覆盖了核心设计（WSA、深度先验、背景策略、零初始化），但未进行不同骨干网络或更大规模基线的对比；未报告统计显著性检验；多视角训练数据规模较小且未公开；消融中未单独验证MWSU的整体效果（数据引擎是否必要），而是隐含在完整框架中。总体来说，实验设计合理，但对比方法仅有两个，且均非严格意义上的多智能体开放域世界模型（MultiWorld限于Minecraft，VerseCrafter为单视角），因此对比范围可进一步扩展。

## 6. 主要结论与发现

1. **数据瓶颈被打破**：MWSU使得从单视角视频直接获取多智能体训练信号成为可能，完全绕过多相机拍摄需求。
2. **跨视角一致性显著提升**：WSA模块通过逐帧分支间交叉注意力，比独立生成方法在CVC上提升约10个百分点（Full-3D场景下从0.7210提升至0.8454）。
3. **身份保真度优异**：SAWG通过外观锚定机制，在主体一致性指标上达到0.9335（Full-3D），优于VerseCrafter（0.9299）和MultiWorld（0.7999）。
4. **背景策略影响关键**：消融实验表明，缺少完整背景（w/o Holistic BG）导致CVC从0.8001暴跌至0.5513，说明全局3D先验对多视角同步至关重要。
5. **零初始化重要性**：不使用零初始化（w/o ZeroInit）导致几乎所有指标下降约20%，证明该设计对保护预训练先验的必要性。

## 7. 优点

- **创新性**：首次实现从单视角视频扩展到开放域多智能体视频世界模型，解决数据稀缺的根本难题。
- **技术优雅性**：MWSU将复杂的多智能体数据需求转化为可自动化的几何分解，WSA采用逐帧交叉注意力，计算高效且可扩展到N个智能体。
- **身份与运动解耦**：通过SAWG的几何接地和外观锚定分离，用户可独立控制“谁”在“哪里”移动。
- **完备的实验设计**：同时评估单视角质量和多视角一致性，消融实验针对每个关键组件。
- **泛化性验证**：展示了多场景（室内、户外）和角色换装（零样本身份替换）能力。

## 8. 不足与局限

- **交互建模缺失**：论文明确承认当前框架未建模智能体与环境或智能体之间的主动交互（如物体操控、身体接触）。
- **朝向表示简化**：智能体朝向直接由瞬时速度导出，不支持原地旋转；需引入显式朝向角。
- **多视角训练数据规模有限**：WSA仅使用小规模真实多视角数据集训练，可能影响在极端复杂场景下的泛化。
- **无公开数据集与代码**：数据引擎依赖多个第三方模型（MoGe-2、SAM 2、Depth Anything），整体复现成本较高；附录中虽提供了部分超参数，但未开源模型权重。
- **实验对比范围窄**：仅与两个基线比较，且其中一个（MultiWorld）仅限于特定游戏环境，另一个（VerseCrafter）本质是单视角模型。缺少与最新多视角生成方法（如4D高斯渲染）的对比。
- **CVC指标局限性**：CVC基于单目深度重建点云，精度受单目深度估计误差影响，可能无法完全反映真实物理一致性。
- **伦理风险**：可生成合成多相机监控场景，可能被滥用；论文建议进行访问控制，但未提出具体机制。

（完）
